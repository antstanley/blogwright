---
'blogwright-analytics': patch
---

Add a Path filter across all reports, matching the exact path and its slash-delimited descendants. Bind literal path values in SQL alongside UTC time and bot filters. Support trailing slashes and clearing the filter, reject malformed paths, and update mock reports to demonstrate scoped results.
