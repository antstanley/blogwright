# Task 01 - Reset PR #27's three paths, keep this spec's own two, and close that PR unmerged

**Plan:** [plan.md](../plan.md) · **Certificate:** [01-supersede_pr27_working_tree-certificate.md](01-supersede_pr27_working_tree-certificate.md)

**Implements:** [2026-08-31-analytics_owned_log_groups.md §Merge plan](../../../changes/2026-08-31-analytics_owned_log_groups.md) step 1 (close PR #27 unmerged, reset that PR's three paths only, keep the `.specs/README.md` registration and the lessons block, and reword the lessons block's node-count sentence), and its Decision *Own the group rather than grant `logs:CreateLogGroup`*, which is what makes that PR's diff unnecessary rather than merely redundant
**Depends on:** -
**Produces:** a working tree carrying none of PR #27 - `git diff packages/ .changeset` empty, the transform role's inline policy back to its two actions and its test back to matching - with this spec's own `.specs/README.md` registration and the 2026-07-26 plan's lessons block both intact, the lessons block's two sentences that describe the superseded fix corrected, and [antstanley/blogwright#27](https://github.com/antstanley/blogwright/pull/27) closed unmerged with a comment naming this spec
**Pointers:** `packages/analytics/src/nodes.ts` (PR #27 rewrites `transformLogGroupArn`'s doc comment, `main@3d47969:900-921`, and edits the logs statement inside `applyTransformRolePolicy`, `main@3d47969:1032` - the net shift is ten lines, so every pointer in this plan below `:921` sits ten lines low until this task lands; cite `main` line numbers only, the spec's working-tree figures are approximate), `packages/analytics/src/nodes.test.ts:1668-1682` (the `toStrictEqual` on the transform role's whole policy document, which PR #27 also edited - resetting only the source would redden it), `.changeset/transform-log-group-grant.md` (PR #27's changeset, staged in the index rather than merely modified, so `git checkout --` alone does not remove it), `.specs/README.md` (KEEP - this spec's standalone pending entry), `.specs/plans/2026-07-26-plugin_system_and_analytics/plan.md` (KEEP - the lessons block appended at the end of §Verification history; its "means a thirteenth node" sentence and its "Fixed by granting it, scoped to the function's own group" sentence both describe the fix this spec supersedes), `.specs/changes/2026-08-31-analytics_owned_log_groups.md` (KEEP - untracked, this spec itself), `.specs/plans/2026-08-31-analytics_owned_log_groups/` (KEEP - untracked, this plan folder, which holds the file you are reading and therefore must exist while this task runs; git reports an untracked directory as a **single** `??` line whatever its kanban subfolders hold, so it is one path in `git status --short` and not one per task file)

## Steps

- [x] Read `git status --short` before touching anything and record it. Three paths are PR #27's and are reset; two tracked paths and **two** untracked paths are this spec's own and are kept - the change spec itself and this plan folder, which git lists as one `??` line for the whole directory. The changeset is **staged** (`A ` in the short status), not merely modified, so removing it takes an unstage as well as a delete.
- [x] Reset exactly `packages/analytics/src/nodes.ts`, `packages/analytics/src/nodes.test.ts` and `.changeset/transform-log-group-grant.md` to `main@3d47969`. Do not run a blanket `git checkout .`, `git restore .` or `git stash`: each would take `.specs/README.md` and the 2026-07-26 plan's lessons block with it, and the second of those is the record of why this change exists.
- [x] Confirm the reset landed on the right side: `packages/analytics/src/nodes.ts` is 3067 lines again, `transformLogGroupArn`'s doc comment at `:912` reads "**No node creates this group.**" (task 03 rewrites it, and rewrites it to something different from PR #27's version), and `applyTransformRolePolicy`'s logs statement at `:1032` is `['logs:CreateLogStream', 'logs:PutLogEvents']`. Do not shortcut this with `grep -rn "logs:CreateLogGroup" packages/analytics/` as a standing check: task 03 adds a comment that names the action in order to say the role is deliberately not granted it, so from task 03 onward that grep matches its own documentation and can no longer fail for the reason it was written.
- [x] Correct the lessons block in `.specs/plans/2026-07-26-plugin_system_and_analytics/plan.md`. Two of its sentences describe PR #27's fix rather than this spec's: "enabling it means a thirteenth node with a lifecycle, a teardown and a retention policy" (this change adds **two** nodes and takes the set to fourteen), and "Fixed by granting it, scoped to the function's own group" (the grant is exactly what this spec declines - the group is owned as a node and the role keeps the two actions it has). Reword both to name this spec and its plan, and leave the rest of the block alone: its generalizable point about record-level signals versus explanatory logs is what this change is built on.
- [x] Close [PR #27](https://github.com/antstanley/blogwright/pull/27) unmerged, with a comment naming this spec as the supersession and stating why: a node that owns the group gets a lifecycle, a retention policy and a teardown, while the grant gets a group that appears at an unpredictable first invocation, retained forever, that nothing reconciles and nothing removes. Do not delete the `fix/analytics-observability` branch as part of this task; closing is the reversible half.
- [x] Run the six gates on the reset tree. This is the point at which their greenness means "main plus this spec's documents", which every later task's baseline claim rests on.

## Definition of done

- [x] `git diff packages/ .changeset` is empty and `git status --short` lists exactly four paths, all under `.specs/`: `.specs/README.md` (modified), `.specs/plans/2026-07-26-plugin_system_and_analytics/plan.md` (modified), the untracked `.specs/changes/2026-08-31-analytics_owned_log_groups.md`, and the untracked `.specs/plans/2026-08-31-analytics_owned_log_groups/`. The fourth is this plan's own folder and is **not** a leftover: it holds this task file, so it is necessarily present while the task runs, and HEAD stays at `3d47969` so it cannot have been committed away. Nothing under `packages/` and no changeset is left over, staged or unstaged.
- [x] The two kept files still carry what this spec put in them: `.specs/README.md`'s Standalone entry for this spec resolves to `changes/2026-08-31-analytics_owned_log_groups.md`, and the 2026-07-26 plan's lessons block is still present at the end of the file rather than truncated by a wide reset.
- [x] The lessons block's two superseded sentences are corrected: it states **fourteen** nodes rather than a thirteenth, and it no longer says the missing-grant finding was "fixed by granting it". A reader arriving from `.specs/README.md` is pointed at this plan rather than at a PR that no longer exists.
- [x] `gh pr view 27 --json state,mergedAt` reports `CLOSED` with a null `mergedAt`, and the closing comment names this change spec. The grant it proposed is nowhere in the tree: the transform role's inline policy statement is `['logs:CreateLogStream', 'logs:PutLogEvents']` and `packages/analytics/src/nodes.test.ts`'s whole-document `toStrictEqual` asserts exactly that, unmodified from `main`.
- [x] Meets the repo definition of done (see plan.md baseline). No changeset: this task changes no shipped behaviour, and the one changeset in play is being removed rather than added.
- [x] Reviewable: run `git status --short`, `git diff --stat packages/ .changeset` and `git log --oneline -1`; confirm the first lists only the four `.specs` paths above, the second is empty, and HEAD is still `3d47969`. Then run `pnpm --filter blogwright-analytics exec vitest run nodes --reporter=verbose` and confirm the `analytics-transform-role` policy case passes against the two-action statement, and `gh pr view 27` to confirm the PR is closed unmerged.

## Open questions

- The `fix/analytics-observability` branch is kept after the PR is closed, on the reading that a closed PR is reversible and a deleted branch is less so. Whether it is deleted once this plan reaches task 05 is the owner's call, not this task's.
