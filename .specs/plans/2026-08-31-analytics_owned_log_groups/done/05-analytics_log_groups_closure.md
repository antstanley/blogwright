# Task 05 - Amend the merged analytics spec, refresh its stale citations, and close this change spec

**Plan:** [plan.md](../plan.md) · **Certificate:** [05-analytics_log_groups_closure-certificate.md](05-analytics_log_groups_closure-certificate.md)

**Implements:** [2026-08-31-analytics_owned_log_groups.md §Merge plan](../../../changes/merged/2026-08-31-analytics_owned_log_groups.md) steps 2 through 7 (step 1 landed at task 01), §Affected spec pages (all five rows) and §Proposed changes (all five blocks applied to the document they amend), plus the changeset the six operator-visible resources require
**Depends on:** 03, 04
**Produces:** [merged/2026-07-26-analytics_plugin.md](../../../changes/merged/2026-07-26-analytics_plugin.md) carrying the two new node rows, the restated counts, the new §Observability block and the corrected `logsUsEast1` sentence, its thirteen stale `file:line` prose citations refreshed and the one superseded-prose citation at `:130` left alone with its exemption recorded; this change spec flipped to `Merged` with a date, moved to `.specs/changes/merged/` with its links re-depthed and its four open questions carried; `.specs/README.md`'s Standalone pending list emptied and a merged entry added beneath the analytics one; and a changeset stating what an existing environment gains on its next `analytics bootstrap`
**Pointers:** `.specs/changes/merged/2026-07-26-analytics_plugin.md` (the document amended in place - §Analytics pipeline → Resource nodes, → Region pinning, → Observability (new), §Analytics plugin → Namespace and commands, §`blogwright-core` → `LogsClient` delivery configuration), `.specs/changes/2026-08-31-analytics_owned_log_groups.md` §Proposed changes (the five quoted blocks, whose relative links are written to resolve from `.specs/changes/` and each gain one `../` as they land in `.specs/changes/merged/`) and §Merge plan step 3 (the thirteen verified citation mappings and the one recorded exemption), `.specs/README.md` §Change specs (the Standalone pending entry this task removes and the merged entry it adds beneath the analytics one), `.specs/plans/2026-07-26-plugin_system_and_analytics/done/30-pds_migration_closure.md` and `done/58-analytics_docs_and_closure.md` (the two closure precedents, including recording a merge-plan step as not-applicable with a reason and an owner rather than passing over it), `.changeset/config.json` (`fixed` - the four published packages version together, so one changeset on `blogwright-analytics` moves all four)

## Steps

- [x] Apply the five `Proposed changes` blocks to `.specs/changes/merged/2026-07-26-analytics_plugin.md`: the fourteen-row Resource nodes table with the two chain-head paragraphs and the site-graph departure note, the new §Observability block, the Region pinning sentence and the corrected `ctx.clients.logsUsEast1` sentence ("four consumers where there were two"), the "orphaning fourteen resources" sentence in §Namespace and commands, and the `ensureLogStream` paragraph in §`LogsClient` delivery configuration. Bump nothing else in that file's header: it is already `Merged`, and this change's own record is what dates the amendment.
- [x] Re-depth every relative link inside the quoted blocks as they land. They are written to resolve from `.specs/changes/` and each needs one more `../` in `.specs/changes/merged/`: `../../packages/analytics/src/nodes.ts` becomes `../../../packages/analytics/src/nodes.ts`, and so on for every `packages/…` reference in the five blocks. Resolve each one after moving it rather than counting segments by eye.
- [x] Refresh the **thirteen** stale `file:line` citations in that document's **prose**, each verified by symbol against `main@3d47969`. Into `packages/core/src/aws/logs.ts`: `:131` → `:172` (the `.find()` in `findDeliveryIdBySource`, `:165`), `:164-171` → `:211-218` (`deleteDeliverySource`), `:139` → `:180` (`deliveriesForSource`, cited twice - §Two guards on the site's node and the Assumptions block), `:114` → `:146` (`createDelivery`), `:71` → `:95` (`filterEvents`). Into `packages/core/src/clients.ts`: `:68` → `:80` (`secrets`), `:28-33` → `:38-43` (`logsUsEast1` and the CloudFront quirk). Into `packages/core/src/aws/endpoint.ts`: `:36,43,65-66` → `:53,80,102-103`. Into `packages/cli/src/nodes.ts`: `:713` → `:766` (`logDeliveryNode`, cited twice - §Motivation and §Two guards on the site's node), `:758` → `:817` (the `ConflictException` retry's `deleteDeliverySource` call, which is the one that sentence names; `delete()`'s own is `:831` and is not what is cited), `:753-757` → `:813-816` (the retry's delivery-deletion loop, which now reaches `deliveriesForSource` through `ownDeliveryIdsOrRefuse` at `:745`), `:732` → `:785` (the `createDelivery` call inside `wire`). Into `packages/core/src/config.ts`: `:352` → `:383` (the `<env>-<siteName>` prefix inside `deriveNames`, which opens at `:379`). Re-verify each by symbol rather than trusting the mapping: `packages/core/src/aws/logs.ts` is exactly the file task 02 edits, so the five mappings into it - all of them below `ensureLogGroup` - shift by the length of `ensureLogStream`. The other eight are into files no task in this plan touches and should still land as written; confirm that rather than assume it.
- [x] Leave **one** prose citation unrefreshed and record it as an exemption, per §Merge plan step 3: `nodes.ts:751-761` at that document's `:130` annotates the sentence "Today it iterates `deliveriesForSource` and deletes every delivery", and no such loop exists on `main@3d47969` - the guard this very block proposed shipped, and the retry now filters through `ownDeliveryIdsOrRefuse`. There is nothing to renumber the pointer *to*, and pointing it at the guarded loop would make a sentence about superseded behaviour cite the code that superseded it. This is the same exemption the §Implementation notes carry, applied to one prose line; name it in the report so a later reader can tell it from a citation nobody checked.
- [x] Settle the one count the spec's five blocks do not reach. `.specs/changes/merged/2026-07-26-analytics_plugin.md:40` is that document's own §Affected spec pages row and reads "Adds a plugin package carrying twelve resource nodes"; the 2026-08-31 spec's §Affected spec pages table does not list it. It is prose describing the package rather than a pointer into it, so the §Implementation notes exemption below does not cover it. Update it or record why not, and name the outcome in the report - a count left at twelve because nobody looked is not the same as one left at twelve on purpose.
- [x] Leave that document's own §Implementation notes alone, and record why in this task's report: they are a pre-implementation record whose pointers are against the tree as it was, and several say so outright. Renumbering them to the shipped tree would make a historical instruction describe a state it was written to change. This step refreshes pointers, not the prose they support.
- [x] Record merge-plan steps 4 and 5 as **not applicable, with a reason and an owner**, rather than passing over them - the shape `done/30-pds_migration_closure.md` and `done/58-analytics_docs_and_closure.md` both use. Step 4, no schema fold: this change has no `Type changes` entry with a schema fragment, `IcebergDestinationInput` being a TypeScript interface internal to the plugin's Firehose client rather than a canonical entity. Step 5, no `DEVELOPMENT.md` edit: no new port, no new toolchain entry, no change to the package split. Owner for both: Ant Stanley.
- [x] Flip this change spec's `Status:` to `Merged`, add a `Merged:` date, move it to `.specs/changes/merged/`, re-point every relative link inside the moved file at the new depth (including its link to the plan folder and to the merged analytics spec, which becomes a sibling), and carry its four open questions forward - three from the spec and the plan's own question about whether the stream node's guard should compare the whole live destination rather than a growing list of fields.
- [x] Update `.specs/README.md`: remove the Standalone entry for this spec, add it to the merged list beneath the analytics entry it amends, and check that the Standalone heading and its introductory sentence still read correctly with nothing under it - the sentence above the list counts what is pending and will be wrong if only the entry is deleted.
- [x] Write the changeset. It is user-facing: an existing environment's next `analytics bootstrap` creates two log groups it did not have, applies a 365-day retention, creates the `DestinationDelivery` log stream, adds a fifth statement to the delivery role and issues one `UpdateDestination` against the live stream, which keeps the stream's ARN so the CloudFront delivery is untouched. Say which of those an operator will see and that nothing needs a teardown. The four published packages are one fixed group, so this versions all four.

## Definition of done

- [x] All five `Proposed changes` blocks are applied to `.specs/changes/merged/2026-07-26-analytics_plugin.md`, including the new §Observability block; the node table has fourteen rows with each group at the head of its chain, and every count in that document that names the set - the table, Region pinning, Namespace and commands - says fourteen. That document's header is unchanged.
- [x] Every relative link inside the applied blocks resolves from `.specs/changes/merged/`, each having gained one `../`, and every link in the moved change spec resolves from its new location. Checked by resolving each path against the filesystem, not by counting `../` segments: a link with the wrong depth renders as a link and fails only when followed.
- [x] The thirteen stale prose citations are refreshed and each was re-verified by symbol against the shipped tree rather than copied from the mapping - `packages/core/src/aws/logs.ts` in particular, since task 02 inserts a method into it and all five mappings into that file sit below the insertion point. The one exemption, `nodes.ts:751-761` at `:130`, is left unrefreshed and named in the report with its reason: its referent is gone rather than moved. That document's own §Implementation notes are left unrefreshed, and this task's report states that as a decision with its reason rather than as an omission. The §Affected spec pages row at `:40`, which the spec's blocks do not reach and the notes exemption does not cover, is settled explicitly one way and the outcome is named.
- [x] Merge-plan steps 4 and 5 are recorded as not-applicable, each with its reason and an owner. A step passed over silently is indistinguishable from a step forgotten, which is what the two closure precedents exist to prevent.
- [x] This change spec is `Status: Merged` with a `Merged:` date, sits at `.specs/changes/merged/2026-08-31-analytics_owned_log_groups.md`, and carries its four open questions; `.specs/README.md`'s Standalone pending list is empty (with its introductory sentence corrected, not merely its entry deleted) and a merged entry sits beneath the analytics one it amends.
- [x] A changeset states the semver impact and the operator consequence: two new log groups, a 365-day retention, a new log stream, a fifth statement on the delivery role, and one in-place `UpdateDestination` against the live stream that keeps its ARN so the CloudFront delivery is untouched. Meets the repo definition of done (see plan.md baseline).
- [x] Reviewable: run `ls .specs/changes .specs/changes/merged` and confirm this spec has moved with a `Merged:` date; open `.specs/changes/merged/2026-07-26-analytics_plugin.md` and confirm the node table has fourteen rows and a §Observability block; follow every relative link in both files and in the changed part of `.specs/README.md` and confirm each resolves; then run `grep -n "twelve" .specs/changes/merged/2026-07-26-analytics_plugin.md` and confirm every remaining hit is one the task deliberately kept and named - the two inside that document's own §Implementation notes (`:555-556` before this task's edits), and the §Affected spec pages row at `:40` only if the report records the reason it stayed. A hit nobody named is a count nobody looked at.
