# Task 60 - Warn at `blogwright bootstrap` while a plugin's scoped state exists, and merge the pds spec

**Plan:** [plan.md](../plan.md) · **Certificate:** [60-cli_bootstrap_plugin_state_warning-certificate.md](60-cli_bootstrap_plugin_state_warning-certificate.md)

**Implements:** [2026-07-26-migrate_pds_to_plugin_system.md §`blogwright-cli` → `bootstrap` warns while plugin state exists (Add)](../../../changes/2026-07-26-migrate_pds_to_plugin_system.md) (the settled 2026-07-27 answer to the spec's bootstrap-warning open question: read the site bucket's `state/` prefix, the same read the destroy refusal performs, and warn per scoped key) and its Decision *The bootstrap warning reads state keys - not config, and not the plugin registry*, plus §Merge plan steps 4–5, deferred here through tasks 30 and 59 because this task lands the spec's last outstanding block
**Depends on:** 16, 59

**Blocked:** behind task 59, which is parked (see `blocked/59-cli_drop_pds_from_site_graph.md`). Two of this task's DoD lines depend on task 59 having landed, and neither can be satisfied while it has not. The changeset line requires this to "ship no later than the release carrying task 59" and makes a reviewer treat the reverse ordering as broken - so this task cannot be dispatched into a branch task 59 is deliberately held out of. And the spec merge requires §The site graph drops its pds branch to have landed, since a change spec is not `Merged` while one of its `Proposed changes` blocks is outstanding; task 59 *is* that block.

This is inherited, not independent: nothing about the bootstrap warning itself is blocked. **To unpark:** unpark task 59 first (cut and publish the release carrying task 30's changeset), then dispatch this task normally. It and task 59 then ship in one release, which is exactly what its changeset line asks for.
**Produces:** after a successful reconcile, `blogwright bootstrap` prints one warning per `state/<env>.<plugin>.json` in the site bucket - naming that plugin and its `blogwright <plugin> bootstrap` - with no discovery, no plugin module load and an unchanged exit code; and the pds change spec merged now that its last `Proposed changes` block has landed
**Pointers:** `packages/cli/src/commands.ts:44` (`bootstrap`, the site verb the warning joins, after its `applyGraph` call), task 16's destroy-guard listing (the `state/` prefix read and the `state/<env>.<scope>.json` matcher in `packages/cli/src/commands.ts` / `plugin-commands.ts` - the helper this task reuses rather than re-implements), `packages/core/src/aws/s3.ts:180` (`listObjects`, the one call the check costs), `packages/core/src/state.ts:17` (`stateKey` - the scoped-key shape task 04 defined), `packages/cli/src/nodes.ts:840-842,962` (`applyOidcRole` rewriting the whole `<env>-deploy` document - the rewrite the warning backs once task 59's removal ships), `packages/cli/src/logger.ts:19` (`logger.warn` renders in plain sessions with colors stripped - nothing prompts), [the pds change spec](../../../changes/2026-07-26-migrate_pds_to_plugin_system.md) (its `Status:` line, §Upgrading a deployed stack item 1, and its §Merge plan steps 4–5), `.specs/README.md` §Change specs (the pending list, whose pds entry this task removes - line anchors are deliberately not given for a file this plan's own tasks keep editing), `.specs/changes/merged/` (the destination this task moves the file to)

## Steps

- [ ] Reuse the scoped-key listing task 16's destroy guard performs - list the bucket's `state/` prefix through `ctx.clients.s3.listObjects` and match `state/<env>.<scope>.json` - extracting it into one named helper if task 16 left it inline, so the key matcher has exactly one home and the two guards cannot drift.
- [ ] After `applyGraph` succeeds in the site's `bootstrap` (`packages/cli/src/commands.ts:44`), call the helper and emit one `ctx.logger.warn` line per scoped key: this bootstrap reconciled only the site's resources, the named plugin's may now be stale, and `blogwright <plugin> bootstrap` re-reconciles them - the plugin name parsed from the key itself, never from config or a plugin module.
- [ ] Keep the check exactly as cheap and lazy as the spec states: one `listObjects` call, no discovery, no plugin module load, and a warning rather than a refusal - the exit code is unchanged and nothing prompts, so `--plain` and non-interactive sessions read the same lines through `logger.warn`.
- [ ] Extend the laziness coverage: the test that pins `bootstrap` loading no plugin module (task 10's) still passes with the warning in place.
- [ ] Write the tests in the suite that covers `bootstrap` (or `plugin-commands.test.ts` beside the destroy-guard cases): with `state/<env>.pds.json` present the run warns naming `blogwright pds bootstrap`; with `state/<env>.pds.json` and `state/<env>.analytics.json` present it warns once per key; with none present the output is byte-identical to today and the call sequence is unchanged except for exactly one added `listObjects` on the `state/` prefix; exit code 0 in every case.
- [ ] Write the changeset for the new warning, and state the release constraint: this ships no later than the release carrying task 59, because that release's `blogwright bootstrap` is the first to rewrite `<env>-deploy` without the site's pds statement, and the warning is what §Upgrading a deployed stack's first item now points an operator at in the terminal.
- [ ] Execute the pds spec's §Merge plan steps 4–5, deferred here through tasks 30 and 59 because this task lands its last outstanding block: flip its `Status:` to `Merged` with a `Merged:` date, move it to `.specs/changes/merged/`, re-point every relative link inside the moved file (`../../packages/…` and `../../DEVELOPMENT.md` gain a level; its link to the plugin-system spec becomes `merged/`-sibling-relative since task 58 moved that one, while its link to the analytics spec still needs `../` until task 61 moves it), carry its three open questions forward per task 30's recording, and remove its entry from `.specs/README.md`'s pending list - leaving the analytics spec as the one pending entry, named as task 61's.

## Definition of done

> **ORCHESTRATOR AMENDMENT - 2026-08-31.** Two obligations were added here after task 58's gate, and
> both were homeless before it. **The plugin-system spec's flip** was task 58's, but task 58 cannot
> discharge it: the spec's §Plugin SPI topography invariant is violated by `packages/cli/src/nodes.ts:971`
> until task 59 lands, and task 59 is parked behind a release. This task ships with task 59, so it is the
> first contract that *can* hold it. The old line here expected `.specs/README.md` to hold exactly one
> pending entry, which was arithmetically incompatible with owning the flip - that is fixed rather than
> worked around. **The `reference/cli.md` documentation gap** had been routed three times (task 20's D6,
> task 30's D6, task 58's decline) and each time landed in no definition of done; task 58 assigned it here,
> but this file did not mention the page, which would have made it a fourth routing into nothing. It is now
> a checkable line. A recorded finding that no DoD names is a finding that gets lost.
>
> Note for whoever discharges the first line: **there is a second `ctx.config.pds` site outside task 59's
> scope** - `packages/cli/src/commands.ts:278`, the rollback PDS-sync warning. It is a command rather than
> a node, so the invariant's literal wording ("no config key of a plugin's is read by a site node")
> survives it and the grep above passes while it stands. Settle it explicitly rather than discover it.


- [ ] After a successful reconcile, `blogwright bootstrap` warns once per `state/<env>.<plugin>.json` in the site bucket, naming the plugin and `blogwright <plugin> bootstrap`, and with no scoped key present its output is byte-identical to today and its call sequence unchanged except for exactly one added `listObjects` on the `state/` prefix - both directions asserted on a recording S3 client and a capturing logger, with the multi-plugin case warning once per key.
- [ ] The check reads key names and nothing else: the matcher is the same helper the `blogwright destroy` refusal uses (one home, asserted by grep for a single definition), no discovery runs and no plugin module loads (task 10's laziness pinning still passes), the exit code is unchanged, and the lines go through `logger.warn` so `--plain` sessions see them without a prompt.
- [ ] The warning is honest about its limit, where the code and changeset record it: a stack that never ran a plugin's bootstrap has no scoped key and sees no warning, so the release notes' `blogwright pds bootstrap` instruction (task 30) is complemented, not replaced.
- [ ] The changeset records the new warning and states that this ships no later than the release carrying task 59 - a reviewer who finds task 59 scheduled for a later release than this task treats the ordering as broken.
- [ ] **BOTH** the pds change spec and the plugin-system change spec have `Status: Merged` with a `Merged:` date, sit in `.specs/changes/merged/` with every relative link re-pointed and their open questions carried, and `.specs/README.md`'s pending list holds exactly one entry - the analytics spec, naming task 61 as its owner. The plugin-system flip additionally requires `grep -n "config\.pds" packages/cli/src/nodes.ts` to return nothing, which is true only once task 59 has landed; if it still matches, this task is not done.
- [ ] `docs/src/content/docs/reference/cli.md` documents the plugin surface - `grep -c plugin docs/src/content/docs/reference/cli.md` returns non-zero - and its §Invocation positional-layout table and §Exit codes row no longer describe pre-migration dispatch. This is the release at which that page stops being merely stale and becomes actively wrong, because the plugin surface ships in it.
- [ ] Meets the repo definition of done (see plan.md baseline).
- [ ] Reviewable: run `pnpm --filter blogwright exec vitest run commands plugin-commands --reporter=verbose`; confirm the with-key case warns naming the verb, the without-key case is byte-identical to today, and the scoped-key matcher greps to one definition; then run `ls .specs/changes .specs/changes/merged` and confirm the pds spec has moved with a `Merged:` date and every link in `.specs/README.md` resolves.
