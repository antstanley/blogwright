# Done Certificate - Task 36: LambdaClient in blogwright-analytics, distinct from MicrovmsClient

**Task:** [36-analytics_lambda_client.md](36-analytics_lambda_client.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-08-30

> This certificate is a verification protocol for Task 36. A validating agent discharges it:
> for each obligation, collect the named evidence, run the named checks, set the Status, then
> derive the Conclusion by the rubric below. Do not mark an obligation SATISFIED without its
> evidence; do not record DONE with any non-SATISFIED obligation.

## Definition

DONE(Task 36) ≡ every obligation O1…O6 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) - not by assertion.

## Premises

- **P1 - Goal.** A `LambdaClient` for the standard Lambda function API sharing the host and signing name with `MicrovmsClient` but none of its paths, with the relationship stated in a module doc comment and proved by a test asserting no request carries the `/2025-09-09/` prefix.
- **P2 - Obligations.** The task is done iff O1…O6 all hold. One Oi per definition-of-done item, in DoD order; O6 is the `Reviewable:` item.
- **P3 - Invariants.** Must not break the MicroVM builder path - `packages/core/src/aws/microvms.ts` (`API` at `:13`, `PATHS` at `:15-21`) and its callers in `packages/cli/src/nodes.ts:287,337,353,362,379,389`, `packages/cli/src/deploy.ts:75,252,273` and `packages/cli/src/microvms.ts:22,60` - nor the barrel export surface consumed by `packages/cli/src/context.ts:5`.

> **Two of this certificate's own checks were authored against premises the task contract
> later contradicted; both are corrected here rather than followed literally.**
> 1. **O2's *Checks* line** says to confirm the client passes "the `lambda` key added at
>    `packages/core/src/aws/endpoint.ts:19`". The task contract's Pointers say the opposite -
>    `SIGNING_NAMES` "gains NO `lambda` key - the plugin supplies
>    `{ service: 'lambda', signingName: 'lambda' }` as a descriptor through task 31's seam".
>    Discharged against the contract. Verified: `endpoint.ts:19-31` has no `lambda` key (only
>    `microvms: 'lambda'`), and `lambda.ts:96` passes a `ServiceDescriptor`.
> 2. **O4's and O6's evidence lines** and the third regression row expect "the one-line
>    `LambdaClient` back-reference this task adds to the `MicrovmsClient` doc comment". That is
>    the task's **Step 6, STRUCK 2026-08-30**. Under the strike the correct expectation is an
>    **empty** `packages/core` diff, and that is what was checked.

## Obligations

- **O1 - The surface is exactly what the transform-function node needs.**
  - *Claim:* `LambdaClient` in `packages/analytics/src/aws/lambda.ts` declares `createFunction`, `getFunction`, `updateFunctionCode`, `updateFunctionConfiguration` and `deleteFunction` and no other public method, each with a typed input rather than an untyped object.
  - *Evidence collected:* `packages/analytics/src/aws/lambda.ts:307-469` declares exactly those five plus the private `call<T>`. Typed inputs: `FunctionConfigurationInput` (`:131-152`, the six configuration fields with units in doc comments - MB, seconds) and `CreateFunctionInput` (`:155-166`, adding `name` and `zipFile`); outputs `LambdaFunction` (`:187-193`) and the `FunctionState` union (`:176-185`). No untyped object crosses the surface. Cross-checked against task 50's contract (`backlog/50-nodes_transform_role_and_function.md`): its function node needs read (`getFunction`), create, hash-gated code and configuration updates, and a delete - all five are consumed, none is speculative. `Tags`, `Publish`, `Qualifier`, `RevisionId` and `CodeSha256` are deliberately absent with the reason recorded at `:74`, `:52-55`, `:196-206` and `:335-338`.
  - *Falsifiability:* mutation **M22** (add a sixth prototype method `invokeFunction`) fails exactly `declares exactly the five operations the transform-function node needs` and nothing else. The surface is pinned, not merely reviewed.
  - *Status:* ☑ SATISFIED

- **O2 - The two clients are documented and provably disjoint.**
  - *Claim:* `packages/analytics/src/aws/lambda.ts` opens with a doc comment naming the shared host and signing name and the `/2025-09-09/` split, and `packages/analytics/src/aws/lambda.test.ts` asserts that every recorded request URL starts with `/2015-03-31/functions` and that none contains `/2025-09-09/`.
  - *Evidence collected:* the module doc comment at `lambda.ts:3-95` states the shared host (`lambda.<region>.amazonaws.com`), the shared SigV4 signing name (`lambda`), `MicrovmsClient`'s exclusive ownership of `/2025-09-09/`, and this module's `/2015-03-31/functions`. `lambda.test.ts:221-241` records all five operations and asserts, over every URL, `toContain('/2015-03-31/functions')`, `not.toContain('/2025-09-09')` and `new URL(url).host === 'lambda.us-east-1.amazonaws.com'`. Suite green: `pnpm --filter blogwright-analytics exec vitest run lambda --reporter=verbose` → **30 passed (30)**.
  - *Checks (corrected per the note above):* `lambda.ts:96` passes `SERVICE: ServiceDescriptor = { service: 'lambda', signingName: 'lambda' }` through task 31's seam. `packages/core/src/aws/endpoint.ts:19-31` has **no** `lambda` key. Host resolution verified in `canonicalHost` (`endpoint.ts:100-114`): `microvms` takes the explicit `case 'microvms': return \`lambda.${region}.amazonaws.com\`` branch, `lambda` takes the `default: return \`${service}.${region}.amazonaws.com\`` branch - the same host by two documented routes, exactly as `lambda.ts:20-27` claims.
  - *Reference verification (outward, before tests).* Every method and path checked against the AWS Lambda API reference, not against the implementation:
    - `CreateFunction` - `POST /2015-03-31/functions`; **required members are `Code`, `FunctionName`, `Role` only** (`Runtime`/`Handler` are Required: No); the package nests as **`Code.ZipFile`**; response is a top-level `FunctionConfiguration` (201).
    - `UpdateFunctionCode` - **`PUT`** `/2015-03-31/functions/{FunctionName}/code`; `FunctionName` is a **URI parameter**, absent from the body; the package is a **top-level `ZipFile`**.
    - `UpdateFunctionConfiguration` - **`PUT`** `/2015-03-31/functions/{FunctionName}/configuration`; `FunctionName` is a **URI parameter**, absent from the body; `Role`/`Runtime`/`Handler`/`MemorySize`/`Timeout`/`Environment` are body members.
    - `GetFunction` - `GET /2015-03-31/functions/{FunctionName}`; response top-level keys are **`Code`, `Concurrency`, `Configuration`, `Tags`, `TagsError`** - the configuration is **nested under `Configuration`**.
    - `DeleteFunction` - `DELETE /2015-03-31/functions/{FunctionName}`; no request body.
    Both silent-success asymmetries the implementer reported are **confirmed independently** and both are pinned by tests proved able to fail: **M1** (un-nest `Code.ZipFile` on create) fails exactly the two create-body cases; **M2** (nest `ZipFile` under `Code` on update) fails exactly the `UpdateFunctionCode` case; **M11** (read the configuration off the response root) fails exactly the two `GetFunction` mapping cases. **M3** (add `FunctionName` to the configuration body) fails exactly the configuration-PUT case, so the URI-parameter rule is pinned too.
  - *Falsifiability of the negative assertion (the DoD's own bullet).* **M7** (make only `PATHS.functions` emit `/2025-09-09/functions`) fails the negative-prefix case; **M7b** (`API = '/2025-09-09'`) fails it plus all six path pins. The vacuity guard is load-bearing and was proved so directly: **M26** (fixture `all()` returns `[]`, guard present) **fails** the negative-prefix case at `toHaveLength(5)`; **M29** (same empty list, `expect(urls).toHaveLength(5)` deleted) leaves it **passing vacuously**. The guard is the only thing standing between this assertion and the failure mode this build has hit five times.
  - *Status:* ☑ SATISFIED

- **O3 - Absence is `undefined`, delete is re-runnable, other failures keep context.**
  - *Claim:* `getFunction` returns `undefined` on `AwsError.isNotFound`, `deleteFunction` resolves on `isNotFound`, and any other failure rejects with the operation and function name in the message.
  - *Evidence collected:* `lambda.ts:415-423` (`getFunction`) and `:456-469` (`deleteFunction`) each narrow on `err instanceof AwsError && err.isNotFound` and otherwise call `rethrowWithContext`. Named cases all green: `returns undefined on a 404 ResourceNotFoundException…`, `swallows a 404 so teardown is re-runnable`, `rethrows a 500 with the operation and function name`, `rejects a 500 rather than swallowing it as not-found`, `rethrows a 403 rather than reading a permissions failure as absence`.
  - *Checks:* `rethrowWithContext` (`lambda.ts:319-331`) reconstructs an `AwsError` preserving `service`, `code`, `statusCode` and `requestId` from `packages/core/src/aws/errors.ts:8-21`, prefixing `${operation} "${functionName}": ` onto the message with `stripAwsFraming` (`:307-314`) removing the constructor's own `${service}: ${code} - … (HTTP ${statusCode})` framing so it is not doubled. A non-`AwsError` passes through untouched. Pinned by `does not repeat the AwsError framing when adding context`, which asserts the exact final string.
  - *Routed finding - confirmed against the reference and the code, not taken on report.* `parseError` (`packages/core/src/aws/signer.ts:177-197`) reads a code only from a JSON body's `__type`/`code`/`Code` or an XML `<Code>`, and reads `requestId` only on the XML branch. Lambda's error body carries `Type` (the *category*, `"User"`/`"Service"`) and `message`; the exception name is header-only (`x-amzn-ErrorType`). `Type` is not one of the three keys `parseError` reads, so **`AwsError.code` is `Http<status>` and `requestId` is always `undefined`** - pinned by `sees the conflict only as a degraded Http409 code, never as the exception name` and by the `requestId: undefined` assertion in `carries the lambda service label…`.
  - *The per-operation regimes - each verified against that operation's own error list in the reference.*
    - **`isFunctionAlreadyExists` = `isAlreadyExists || statusCode === 409`, applied to `createFunction` only** (`lambda.ts:304`, used once at `:390`). **M16** (drop the 409 limb) fails **exactly** `resolves on a 409 ResourceConflictException so a re-run of create is a no-op` - one test, no others. `CreateFunction`'s only 409 is `ResourceConflictException`.
    - **409 on delete is not already-exists.** `DeleteFunction`'s 409 is documented as "the resource already exists, **or another operation is in progress**" - on a delete it can only be the latter. `deleteFunction` narrows on `isNotFound` alone. **M17** (add `isFunctionAlreadyExists` as a second limb on delete) fails **exactly** `rejects a 409 rather than reporting a function that is mid-operation as torn down` - one test. Swallowing it would report a live function as torn down.
    - **404 on create is not "function absent"** - it is the execution role (or a layer, or a code-signing config). `createFunction` does not swallow it: **M19** (swallow every `AwsError` as already-exists) fails `rejects a 404 rather than reading the absent execution role as an existing function` plus the 400, 500 and two error-context cases.
    - **`isNotFound` works here through its `statusCode === 404` limb** - its regex is tested against `code`, which is `Http404`, and never matches. **Uniqueness verified from the reference where it is load-bearing:** `GetFunction`'s errors are 400/404/429/500 with **`ResourceNotFoundException` the only 404**; `DeleteFunction`'s are 400/404/409/429/500 with **`ResourceNotFoundException` the only 404 and `ResourceConflictException` the only 409**. Those two operations are the only ones that narrow on a status, so both status limbs are safe.
  - *Fixture honesty.* `errorResponse` (`lambda.test.ts:83-93`) emits `x-amzn-errortype` and `x-amzn-requestid` headers with a body of `{"Type":"User","message":…}` - the shape the live endpoint returns, with no invented body `code`. **M18** (add `__type: code` to the fixture body - task 33's fiction) kills **exactly 3** tests: the degraded-`Http409` case, `carries the lambda service label, the degraded code…` and `does not repeat the AwsError framing…`. The implementer's count is exact. Under that fiction `isAlreadyExists` would match on its `Conflict` alternative and the create-idempotency test would pass **for a reason that does not hold against AWS** - the defect task 33 shipped.
  - *Status:* ☑ SATISFIED

- **O4 - Exported and not dead.**
  - *Claim:* `packages/analytics/src/index.ts` re-exports `./aws/lambda.js`, core gains no `lambda` service key, and `pnpm knip` reports no unused export for the new module.
  - *Evidence collected:* the barrel diff is **exactly one line** - `+export * from './aws/lambda.js';` - inserted between `./aws/glue.js` and `./aws/s3tables.js`, alphabetical. No landed sibling client (`firehose.ts`, `glue.ts`, `s3tables.ts`) was edited; `jj diff --summary` is exactly `A lambda.test.ts`, `A lambda.ts`, `M index.ts`. No duplicate export names across the four `aws/*.ts` modules, so the `export *` union is unambiguous. `packages/core/src/aws/endpoint.ts:19-31` gained no `lambda` key and `packages/core/src/index.ts` is unchanged. **`jj diff packages/core` is empty** - `packages/core` is untouched, which under the STRUCK Step 6 is the correct outcome, not a shortfall. `pnpm knip` from the workspace root: **clean, no output**.
  - *Status:* ☑ SATISFIED

- **O5 - Meets the repo definition of done.**
  - *Claim:* tests written with the change pass, the lint/format/dead-code gates are clean, and limits are named constants or validated config fields.
  - *Evidence collected:* all six `.github/workflows/ci.yml:21-29` gates run from the workspace root, in order - `pnpm build` ✅, `pnpm typecheck` ✅, `pnpm test` ✅ (core, analytics 176, build-agent 27, pds 100, cli 300 - all green), `pnpm lint` ✅ (exit 0; the only warnings are pre-existing `no-shadow` in `packages/cli/src/nodes.test.ts`, untouched by this task), `pnpm exec oxfmt --check .` ✅ ("All matched files use the correct format", 154 files), `pnpm knip` ✅. Constants named rather than inlined: `API` (`:100`), `PATHS` (`:102-115`), `PACKAGE_TYPE` (`:123`), `SERVICE` (`:96`). Units stated in the input doc comments (`MemorySize` in MB with its 128-32768 range, `Timeout` in seconds with the 900 cap). Errors carry context; `undefined`, never `null`, signals absence. No changeset - consistent with landed siblings 33/34/35, none of which shipped one: the package declares no `blogwright.plugin` manifest field and no default export yet, so nothing here is user-facing.
  - *Falsifiability sweep (independent, not the implementer's table).* 27 applied mutations (M1-M29, two re-run after a NOOP), each restored before the next. **Every one of the 30 `it` blocks was observed failing under at least one mutation**, and every mutation was killed. The blast radius was checked per mutation, not just the count: M1→2, M2→1, M3→1, M4→1, M5→1, M6→1, M7→2, M7b→7, M8→1, M9→1, M10→1, M11→2, M12→1, M13→1, M14→3, M15→6, M16→1, M17→1, M18→3, M19→5, M20→1, M21→1, M22→1, M23→1, M24→3, M25→2, M26→2, M27→8, M29→1. One surviving mutant, recorded in Residue.
  - *Restore proved:* `shasum -a 256 -c` against the pre-sweep baseline → both files `OK`; `diff -q` against the backups → identical; `jj st` → still exactly the three-file diff; the suite re-run after the sweep → 30 passed (30).
  - *Status:* ☑ SATISFIED

- **O6 - Run the Reviewable commands; confirm the suite is green, the grep matches only the test's negative assertion, and `packages/core/src/aws/microvms.ts` still owns every `/2025-09-09/` path.**
  - *Claim:* a reviewer can run those two commands and observe a green Lambda suite with no MicroVM path anywhere in the new client's request construction.
  - *Evidence collected:* `pnpm --filter blogwright-analytics exec vitest run lambda --reporter=verbose` → 30 passed, all 30 case names listed. The grep matches **5 lines**: `lambda.test.ts:220` (the case's comment), `:221` (the `it` title), `:236` (`expect(url).not.toContain('/2025-09-09')` - the negative assertion itself), and `lambda.ts:13` and `:98` (module doc comments). `jj diff packages/core/src/aws/microvms.ts` is **empty**; `microvms.ts:13` still holds `const API = '/2025-09-09';` and it is the **only** such constant in the repo. A repo-wide grep confirms `/2025-09-09` appears in production code only in `microvms.ts` and `endpoint.ts:28` (both core, both pre-existing) and in this task's doc comments and negative assertion.
  - *Judgement on the literal wording.* The line says the grep should match "only the test's negative assertion". Taken literally that is unsatisfiable alongside **Step 1**, which *requires* a module doc comment stating that `MicrovmsClient` "addresses the `/2025-09-09/` MicroVM paths", and alongside the DoD's own second bullet, which requires the same of the doc comment. A doc comment cannot state the split without naming the prefix. The line's intent - **no `PATHS` entry carrying the prefix and no request carrying it** - is met and is independently proved by M7/M7b/M26. The two `lambda.ts` matches are mandated prose; the three test matches are one assertion and its label. **Intent satisfied.**
  - *Status:* ☑ SATISFIED

## Regression check

`packages/core` is untouched and `packages/core/src/index.ts` is unchanged, so every MicroVM path is preserved by construction; each row was still traced and the full CLI suite (300 tests) re-run green.

- `packages/cli/src/nodes.ts:287` calls `ctx.clients.microvms.getImage(arn)` → `GET /2025-09-09/microvm-images/<id>` on `lambda.<region>.amazonaws.com` via `canonicalHost`'s `case 'microvms'` branch, unchanged : ☑ PRESERVED  *(the certificate's authored line number 285 had drifted; the call is at :287)*
- `packages/cli/src/deploy.ts:75` calls `ctx.clients.microvms.runMicrovm(input)` → `POST /2025-09-09/microvms`, unchanged : ☑ PRESERVED
- `packages/cli/src/context.ts:5` imports from `blogwright-core` → `packages/core/src/index.ts` unchanged, every named export still resolves; `pnpm typecheck` and `pnpm build` green across all six projects : ☑ PRESERVED

## Residue

- **Struck Step 6 verified as struck, not skipped.** `packages/core` carries no back-pointer and no other edit. Discoverability runs one way - `lambda.ts:11-19` names `microvms.ts`, its `API` constant and its exclusive ownership of `/2025-09-09/` - which is the direction that stays true for a consumer who installs `blogwright-core` alone.
- **One surviving mutant (M28): the SigV4 signing name is not test-pinned.** Changing `SERVICE.signingName` from `'lambda'` to `'execute-api'` kills no test - the recording transport captures method, URL, `content-type` and body, but not `authorization`. Not a correctness defect: the shipped value is right, the host *is* pinned (M27 kills 8 tests), and a wrong signing name fails loudly as a 403 `InvalidSignatureException` rather than deploying silently. It is also the landed precedent - none of `s3tables.test.ts`, `firehose.test.ts` or `glue.test.ts` asserts a credential scope either - and the DoD assigns the signing-name claim to the doc comment, which makes it. Task 50's DoD *does* require a scope assertion (`/us-east-1/secretsmanager/`), so the pattern exists in the plan and could be back-fitted to all four clients in one follow-up.
- **Fourth copy of `stripAwsFraming` + `rethrowWithContext`.** Both now appear in all four `packages/analytics/src/aws/*.ts` clients, in tension with the repo DoD's "no duplicated logic". Following the landed precedent was the right call for this task - extracting a shared helper would require editing three already-landed sibling files - but the extraction is worth a follow-up, and would pair naturally with the `parseError` header fix already in plan.md's open questions.
- **Documentation nit at `lambda.ts:80-81`:** "each of the two this client narrows on belongs to exactly one exception" is true of the operations that narrow (`GetFunction`, `DeleteFunction`) but over-general for the service as a whole - `CreateFunction` and both updates also return 404 for `CodeSigningConfigNotFoundException`. The table's own 404 row already names the create case ("or a layer, or a code-signing config"), and `createFunction` narrows on no status, so nothing load-bearing rests on the over-general sentence.
- **`createFunction` returns `void`** - the ARN is unavailable on the already-exists path, so task 50 will read it back with the `getFunction` it makes before creating anyway, or derive `arn:aws:lambda:<region>:<account>:function:<name>`. Judged sound and consistent with `s3tables.ts`'s `createTableBucket`. No landed task pins a `createFunction` return, so this closes no door.
- **`CodeSha256` deliberately dropped - reasoning checked against the consuming tasks, not accepted.** `backlog/43-transform_bundle_and_source_hash.md`'s DoD states the hash "is computed over the transform's **source**, not the built bundle (DEVELOPMENT.md §Repository hygiene - image keys must not vary by platform)", and `backlog/50-nodes_transform_role_and_function.md` gates its update on "the recorded hash" from task 43 held in the plugin's scoped state. Neither needs the zip digest. **Not a gap.**
- **`LambdaFunction.arn` falls back to `''`** when a 200 carries no `Configuration`, matching `normalizeImage`/`normalizeCatalog`/`normalizeTable`. Documented as neither observed nor documented from the service. If task 50 ever records that empty string as a function ARN it would be a silently wrong state value; the `getFunction` mapping test pins the populated path.
- **Base is one commit behind, and the gap is empty of conflict.** The workspace parent is `nwsoznnm dcfb0ea2` (task 35); during this review **task 52 landed** as `qzunsukw 4dd95d53`, advancing the bookmark. Overlap checked file by file: task 52 touched `packages/cli/src/nodes.ts`, `packages/core/src/aws/logs.ts` and their tests - **zero intersection** with this task's three files, and it touched none of the core modules `lambda.ts` imports (`signer.ts`, `errors.ts`, `endpoint.ts`, `index.ts` all unchanged by it). Its one breaking core change - `LogsClient.deliveriesForSource` now returning `DeliverySummary[]` instead of `string[]` - reaches nothing in `packages/analytics` (grep for `LogsClient`/`deliveriesForSource`/`DeliverySummary` over `packages/analytics/src/` is empty), and its `logs.test.ts` edit is purely additive from `:56` down, leaving the `:9-16` transport helpers this task's tests mirror unchanged. Task 36 rebases cleanly. The change is itself purely additive (one new module, one barrel line) and pins no call sequence, output or shape that any landed task depends on. `LambdaClient`'s first consumer is task 50, still in `backlog/`.
- **This certificate's own O2 `Checks` and O4/O6 evidence lines are stale** and were corrected at the head of Obligations. If the certificate is reused, fix `endpoint.ts:19` → "the descriptor seam, no core key" and drop the "one-line back-reference in core" expectations.

## Conclusion

VERDICT: ☑ **DONE**
CONFIDENCE: ☑ **high**
SUMMARY: All six obligations are SATISFIED on collected evidence - both silent-success wire asymmetries (`Code.ZipFile` vs top-level `ZipFile`; `GetFunction`'s `Configuration` nesting) and all three narrowing regimes were verified against the AWS Lambda API reference before any test was read and are each pinned by an assertion proved able to fail, the `/2025-09-09/` negative assertion's `toHaveLength(5)` guard was proved load-bearing by direct vacuity probe, an independent 27-mutation sweep killed every mutant and observed all 30 `it` blocks failing with restores hash-proved, `packages/core` is untouched as the struck Step 6 requires, and all six repo gates are green from the workspace root.
