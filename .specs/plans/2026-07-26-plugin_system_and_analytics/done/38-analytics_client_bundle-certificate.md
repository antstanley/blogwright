# Done Certificate - Task 38: Build the plugin's four clients over the shared signer, pinned to us-east-1

**Task:** [38-analytics_client_bundle.md](38-analytics_client_bundle.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-08-30

> Discharged against the workspace at `/Users/ant/code/blogwright-task-38` (jj working copy
> `wnksynvl 9f2b4faa`, parent `zxzwmomx cd597013 build(32/62): land task 17`), 11 files changed,
> +455/-121. Every mutation recorded below was reverted; the restore is proved at the end.

## Definition

DONE(Task 38) ≡ every obligation O1…O7 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) - not by assertion.

## Premises

- **P1 - Goal.** `packages/analytics/src/aws/clients.ts` exposes `createAnalyticsClients(ctx)` building `s3tables`, `firehose`, `glue`, `lambda` and a us-east-1 `S3Client` over `ctx.clients.signingUsEast1`, every one signing against `us-east-1` whatever `config.region` says - while core's `AwsClients` gains that one signer and no service key.
- **P2 - Obligations.** The task is done iff O1…O7 all hold. One Oi per definition-of-done item, in DoD order; O7 is the `Reviewable:` item.
- **P3 - Invariants.** Must not break the existing client bundle - every consumer of `createClients` (`packages/cli/src/context.ts:127`, `packages/cli/src/test-support.ts:70`, `packages/pds/src/test-support.ts:50`) and every `ctx.clients.*` call site in `packages/cli/src/nodes.ts`, `packages/cli/src/deploy.ts` and `packages/cli/src/microvms.ts` - nor the region split already encoded at `packages/core/src/clients.ts:52,54`.

## Obligations

- **O1 - Core's `AwsClients` gains exactly one member and no service key.**
  - *Claim:* `AwsClients` at `packages/core/src/clients.ts:21` gains `signingUsEast1` - the signer already constructed as a local `const` at `:54` - and nothing else; the rest of the returned object is byte-identical.
  - *Evidence collected:* `packages/core/src/clients.ts` diffed against `@-` with every `signingUsEast1` line stripped from both sides. The residual difference is **two hunks and nothing else**: the nine-line doc comment on the new interface member, and one word appended to an existing comment (`// ACM for CloudFront must be us-east-1 regardless of the primary region;` + ` the same signer is handed out as signingUsEast1 for plugin-supplied services.`). The returned object is otherwise byte-identical: `region`, `signing`, `s3`, `sts`, `iam`, `logs`, `logsUsEast1`, `acm`, `cloudfront`, `route53`, `microvms`, `secrets` all unchanged, each over the signer it had. `grep -rn "s3tables\|firehose\|glue" packages/core/src --include='*.ts' --exclude='*.test.ts'` → **no output** (exit 1). `jj diff packages/core/src/aws/` → **empty**: `SIGNING_NAMES`, `endpoint.ts` and `signer.ts` are untouched.
  - *Checks:* the value assigned is the existing binding - `signingUsEast1: usEast1` at `:70`, where `usEast1` is the unchanged `const usEast1 = new SigningClient({ region: 'us-east-1', ...base })` at `:64`, so it carries `base`'s `endpointOverride`, `credentials` and injected `transport`. **Falsified by mutation:** replacing it with a hand-built `new SigningClient({ region: 'us-east-1', endpointOverride, credentials, transport: <own stub> })` reddens `exposes a us-east-1 signer that shares the bundle transport` with `expected exactly one signed request, recorded 0` - the identity of the signer, not merely its region, is what the test pins.
  - *Status:* ☑ **SATISFIED**

- **O2 - The plugin's six clients are built in the plugin, over `signingUsEast1`.**
  - *Claim:* `createAnalyticsClients(ctx)` constructs the four new clients with task 31's descriptors plus core's `S3Client` and `SecretsManagerClient`, all six over `ctx.clients.signingUsEast1`, under one doc comment naming the CloudFront constraint.
  - *Evidence collected:* `packages/analytics/src/aws/clients.ts:74-84` - a single `const signing = ctx.clients.signingUsEast1;` feeds all six constructors. `grep -rn "ctx.clients.s3\b\|ctx.clients.secrets" packages/analytics/src/` → **no output** (exit 1). Stronger: `grep -rn "ctx\.clients" packages/analytics/src/` returns **exactly one line**, `clients.ts:75`, so there is no aliasing or destructuring route around the DoD grep either. The doc comment at `:57-73` states the pin once, names CloudFront standard logging as the reason, cross-references core's `logsUsEast1` comment, and explains both the `s3`/`secrets` reconstruction and the private-`credentials`/`transport` reason for not hand-building a signer. The grep correctly omits `ctx.clients.iam` and `ctx.clients.logsUsEast1` - the analytics spec §Region pinning names both as its two stated exceptions, and this task's bundle contains neither.
  - *Checks (region-pin trap, by mutation):* `s3: new S3Client(signing)` → `s3: ctx.clients.s3` reddens **exactly one** test, `signs the error bucket in us-east-1, not with the host s3 client`, on `region: eu-west-1`. `secrets: new SecretsManagerClient(signing)` → `secrets: ctx.clients.secrets` reddens **exactly one**, `signs the salt secret in us-east-1, not with the host secrets client`. Neither leaks into another case.
  - *Prose-rewrite judgement:* the four new-file prose mentions of the host's `S3Client`/`SecretsManagerClient` are phrased as "the host bundle's own copy" / "lifting that one onto the plugin's bundle" rather than with the literal `ctx.clients.s3`. **Honest, not evasive.** The information is fully preserved - each site states the prohibition and its consequence explicitly - while the DoD grep stays a *code* check rather than a prose check. That matters beyond this task: tasks 50/51/53 add nodes to this same package and will re-run the same grep; a prose false positive would have retired it on its first use. The actual defect the grep exists to catch is independently caught by the two mutations above, so nothing rests on the grep alone.
  - *Status:* ☑ **SATISFIED**

- **O3 - A recording-transport test proves the region split both ways, on both sides.**
  - *Claim:* the plugin-side test asserts all six clients scope to `us-east-1` from a `config.region: 'eu-west-1'` context; the core-side test asserts `logs`, `s3`, `microvms` and `secrets` still scope to the configured region and `signingUsEast1` to `us-east-1`.
  - *Evidence collected:* `pnpm --filter blogwright-analytics exec vitest run clients --reporter=verbose` → 6/6 green, one `it` per client, each asserting the parsed `<region>/<service>` pair out of the SigV4 `Authorization` header (`credentialScope`, `clients.test.ts:31-40`), not the URL. `pnpm --filter blogwright-core exec vitest run clients --reporter=verbose` → 5/5 green.
  - *Checks - the signing-name half (the load-bearing one):* mutating **all four** `signingName`s at once (`s3tables.ts:36`, `firehose.ts:43`, `glue.ts:54`, `lambda.ts:96` → `…XX`) leaves the four clients' own suites at **105/105 passing** and reddens **exactly 4 of 6** in `clients.test.ts`, each on the `service` field alone (`- "service": "s3tables"` / `+ "service": "s3tablesXX"`, and the same for firehose, glue, lambda) with `region` unchanged. The implementer's central claim reproduces **exactly**. These six assertions are confirmed to be the only signing-name check in the repo.
  - *Checks - the region half, independently:* `ctx.clients.signingUsEast1` → `ctx.clients.signing` reddens **all six** on `region` (`us-east-1` → `eu-west-1`) with every `service` field intact. The two axes are therefore discriminated separately, not conflated.
  - *Checks - the vacuity guard:* deleting `await clients.glue.getCatalogFederation(...)` from its `it` fails that test with `Error: expected exactly one signed request, recorded 0` (`clients.test.ts:73-75`; core `clients.test.ts:66-68`). A client that signs nothing fails loudly. This is a strict improvement on the `capture()` helper the task's step named, whose `last: () => seen!` would have returned `undefined` and thrown an unhelpful TypeError or, worse, compared unequal; it is also what makes the O1 transport-identity mutation observable at all.
  - *Checks - `microvms` resolves as specified:* `packages/core/src/clients.test.ts:106-113` expects `{ region: 'eu-west-1', service: 'lambda' }` - the signing name, distinguishing it from the plugin's `{ region: 'us-east-1', service: 'lambda' }`. Repointing `logs`/`s3`/`microvms`/`secrets` to `usEast1` reddens **exactly those four** and leaves the fifth green; `signingUsEast1: signing` reddens **only** the fifth. All five core assertions falsifiable and discriminating.
  - *Checks - cross-package coupling is real:* `packages/analytics/vitest.config.ts` aliases `blogwright-core` → `../core/src/index.ts`, so the core mutation `signingUsEast1: signing` also reddens all six analytics tests. The plugin suite exercises core's source, not a stale `dist`.
  - *Status:* ☑ **SATISFIED**

- **O4 - No dead export, no unused dependency.**
  - *Claim:* `pnpm knip` from the repo root reports nothing.
  - *Evidence collected:* `pnpm knip` → no findings, exit 0. `knip.json` is **not in the diff** and contains no `ignoreExports` at all; its single `ignoreDependencies` entry (`blogwright-build-agent` in `packages/cli`) predates this task. The clean report is therefore genuine, not suppressed.
  - *Checks:* `stripAwsFraming` is module-private in `errors.ts` (`grep -rn "stripAwsFraming"` outside that file → nothing), which is what keeps knip quiet without an ignore entry; `rethrowWithContext` is exported and imported by all four siblings. `createAnalyticsClients` is consumed today only by `clients.test.ts`, which is deliberate and documented (`clients.ts:10-15`: not barrelled, because the published surface is what the CLI loads through the SPI). This is **not** the manufactured-consumer anti-pattern the plan's knip bullet warns about: the factory is the task contract's named `Produces:`, and its test genuinely exercises it - proven by six independent mutations that redden it.
  - *Status:* ☑ **SATISFIED**

- **O5 - The changeset decision is recorded.**
  - *Claim:* a `.changeset/*.md` marks a minor on `blogwright-core` covering `signingUsEast1`, or the change description defers it to task 58.
  - *Evidence collected:* `.changeset/core-signing-us-east-1.md` added, `"blogwright-core": minor`. Its text is accurate against the code on every claim: no service client moves, `SIGNING_NAMES` is unchanged, the bundle gains no new service key - all three independently verified under O1. The DoD's "plus the `LogsClient` parameters" is satisfied in substance rather than by duplication: task 37's `.changeset/logs-delivery-output-format.md` already carries the `LogsClient` delivery-configuration parameters and task 31's `.changeset/core-transport-service-descriptor.md` already carries the transport seam, both landed in the parent. Folding them into a third changeset would have double-counted them in the changelog. `.changeset/config.json` puts `blogwright`, `blogwright-core`, `blogwright-pds` and `blogwright-analytics` in one `fixed` group, so the core minor carries the CLI with it - and `packages/cli/src/test-support.ts` is internal (the CLI publishes only `./rkey`), so the `testClients` widening needs no changeset of its own.
  - *Status:* ☑ **SATISFIED**

- **O6 - Meets the repo definition of done.**
  - *Claim:* the six CI gates are green from the workspace root, in `.github/workflows/ci.yml` order.
  - *Evidence collected:* `pnpm build` exit 0 · `pnpm typecheck` exit 0 · `pnpm test` → core 148 passed/1 skipped, build-agent 27, pds 100, analytics 228, cli 317 (all files passing) · `pnpm lint` exit 0 (29 `no-shadow` warnings, every one pre-existing in `packages/cli/src/nodes.test.ts`, none in a file this task touched) · `pnpm exec oxfmt --check .` → "All matched files use the correct format", 160 files · `pnpm knip` exit 0. Re-run after every mutation was reverted, with identical counts.
  - *Checks - the ROUTED FINDING on duplication:* the extraction landed and is behaviour-preserving. `errors.ts` holds `rethrowWithContext` (exported) and `stripAwsFraming` (module-private); all four siblings import it and each lost 30 net lines. The four removed copies were byte-identical modulo the parameter name (`streamName`/`catalogName`/`functionName`/`resource`), and the shared template is the same `${operation} "${resource}": ${stripAwsFraming(err)}`. **Load-bearing for all four, by mutation:** dropping the `${operation} "${resource}": ` prefix reddens **14** tests spread across every sibling suite - firehose 3, glue 2, lambda 6, s3tables 3; breaking `stripAwsFraming` (deleting both slice limbs) reddens **8** - firehose 3, glue 2, lambda 2, s3tables 1. Both numbers reproduce the implementer's claim exactly, and both spreads cover all four modules, so this is a genuinely shared module rather than a dead copy with three live ones.
  - *Checks - what was NOT extracted (the critical negative):* the four already-exists predicates are **not** collapsed. `s3tables.ts:210 isAlreadyExists` keeps `err.isAlreadyExists || err.statusCode === 409`; `lambda.ts:305 isFunctionAlreadyExists` keeps the same 409 limb; `firehose.ts:221 isStreamAlreadyExists` keys on `err.code === 'ResourceInUseException'` with **no** status limb; `glue.ts:239` uses core's `err.isAlreadyExists` unmodified. `errors.ts:23-42` records exactly why, and each limb of that explanation checks out against the sibling module docs (s3tables' `x-amzn-ErrorType`/`Http409` gap at `:187`; firehose's "every one of these is HTTP 400" at `:218-220`). The delete/update hazard is recorded too (`errors.ts:33-41`) and honoured in code: `firehose.deleteDeliveryStream` (`:365-371`) narrows on `isNotFound` only and lets `ResourceInUseException` through; `lambda.deleteFunction` (`:433-438`), `updateFunctionCode` and `updateFunctionConfiguration` swallow nothing. The create-path predicates appear at exactly one call site each.
  - *Status:* ☑ **SATISFIED**

- **O7 - The `Reviewable:` line (corrected form).**
  - *Claim:* a reviewer can run those commands and observe the region split, a clean knip report, and a green CLI node suite with an unedited `nodes.test.ts`.
  - *Evidence collected:* `pnpm --filter blogwright-analytics exec vitest run clients --reporter=verbose` → 6 passed, every case naming its client and asserting `us-east-1`. `pnpm --filter blogwright-core exec vitest run clients --reporter=verbose` → 5 passed, four naming `eu-west-1` (`s3`, `logs`, `secrets`, `microvms`-as-`lambda`) and one naming `us-east-1`. `pnpm knip` → clean. `pnpm --filter blogwright exec vitest run nodes --reporter=verbose` → **33 passed**, and `jj diff packages/cli/src/nodes.test.ts` is **empty** - the fakes are unchanged. The corrected filter (`blogwright`, not `blogwright-core`) is the one that works; `blogwright-core` has no `nodes` test file.
  - *Checks - nothing was added to a pinned sequence:* the only production change outside the analytics package is core's one-key addition (constructs nothing new) and the CLI's one `overrideClient(base.signingUsEast1, overrides.signingUsEast1)` line, which issues no AWS request when the override is `undefined`. CLI 317/317 and pds 100/100 are unchanged from baseline, and the four sibling suites hold at 105/105 across the extraction.
  - *Status:* ☑ **SATISFIED**

## Regression check

- `packages/cli/src/context.ts:127` calls `createClients(...)` → every pre-existing key keeps its signer, `microvms` included; proved directly by `packages/core/src/clients.test.ts` (`microvms` asserted as `/eu-west-1/lambda/`) and by the byte-identity diff under O1 : ☑ **PRESERVED**
- `packages/cli/src/test-support.ts:69` re-wraps each client with `overrideClient` → `signingUsEast1` is enumerated at `:93`, so a `ClientOverrides` substitution is honoured rather than dropped by `...base`. `SigningClient` uses TS `private readonly`, not `#private` fields, so `overrideClient`'s `Object.create(base)` works on it exactly as it already does for `signing` : ☑ **PRESERVED**
- `packages/pds/src/test-support.ts:50` calls `createClients` → `pnpm test` in `packages/pds` green at 100/100 with no edit : ☑ **PRESERVED**
- `packages/cli/src/deploy.ts:75` calls `ctx.clients.microvms.runMicrovm(input)` → signs for the configured region; the new core test pins this and is falsified by repointing `microvms` to `usEast1` : ☑ **PRESERVED**
- *(added)* `packages/cli/src/plugin-commands.ts:322` passes `clients: ops.clients` - the whole `AwsClients` - into `PluginContext`, so `signingUsEast1` genuinely reaches a plugin at runtime and `createAnalyticsClients(ctx)` is not resolving against a hand-rolled subset : ☑ **PRESERVED**

## Residue

Notes carried forward, none of them defects:

1. **The `testClients` widening has no test.** Deleting `packages/cli/src/test-support.ts:93` leaves the CLI suite at 317/317. The certificate's own residue pre-authorised treating a *missing* enumeration as a finding; it is present and correct by construction (identical to `signing`'s line, one row above). Nothing substitutes `signingUsEast1` yet - the first task that does should assert the substitution takes effect.
2. **`credentialScope` is duplicated between the two new test files** (~10 lines, byte-identical). Unavoidable without exporting a test helper from core's published surface, which knip and the public-API rule both argue against. The two `bundle()` helpers differ meaningfully and are not duplication.
3. **The step said "reusing the `capture()` recording transport"; the implementer reimplemented it.** `capture()` lives in `packages/core/src/aws/signer.test.ts` and is not importable across the package boundary, and its `last: () => seen!` is precisely the vacuous shape this build has shipped before. The replacement's exactly-one-request guard is the reason two of this task's assertions are falsifiable at all. A deviation that improved the step.
4. **The endpoint override reaching the plugin's clients is structural, not asserted.** `signingUsEast1` *is* the `usEast1` object built from `base`, so `AWS_ENDPOINT_URL`/floci reaches it by identity; the transport-identity test pins the same property through the transport. No separate `--endpoint` assertion exists.
5. **`AnalyticsClients.s3` and `.secrets` share their key names with `AwsClients`.** A later node writing `ctx.clients.s3` compiles and silently uses the primary-region client. The DoD grep is the standing guard for that as tasks 50/51/53 add nodes - which is the second reason keeping it free of prose false positives was the right call.
6. **The working-copy change description is empty** (`no description set`); the builder sets it at merge. The changeset file, not the description, discharges O5.

## Conclusion

VERDICT: ☑ **DONE**
CONFIDENCE: ☑ **high**
SUMMARY: All seven obligations are SATISFIED with executed evidence and all five regression traces PRESERVED - the six CI gates are green from the workspace root, the corrected `Reviewable:` line runs clean in both packages, and ten independent mutations (four signing names at once → 105/105 sibling tests still green while 4 of 6 header assertions redden on `service`; the region swap → all six redden on `region`; each host-client reuse → exactly one named test; a deleted client call → the vacuity guard; the prefix drop → 14; the framing break → 8; four core repoints → 4; the signer swap → 1; a hand-built signer → the transport-identity case) reproduce every claim the implementer made, exactly and without overstatement.
