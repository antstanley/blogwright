# Task 54 - Assemble the twelve-node graph and run it through the plugin lifecycle verbs

**Plan:** [plan.md](../plan.md) · **Certificate:** [54-analytics_graph_and_lifecycle_commands-certificate.md](54-analytics_graph_and_lifecycle_commands-certificate.md)

**Implements:** [2026-07-26-analytics_plugin.md §Analytics pipeline → Resource nodes (Add)](../../../changes/merged/2026-07-26-analytics_plugin.md) (the twelve-row node table) and §Analytics plugin → Namespace and commands (Add) ("The plugin's resources live in its own scoped state store (`state/<env>.analytics.json`), so `blogwright destroy` neither provisions nor removes them") and §Analytics pipeline → Shape (Add) and §Analytics pipeline → Region pinning (Add) ("The plugin states this at bootstrap rather than deriving it silently")
**Depends on:** 16, 47, 50, 53
**Produces:** `buildAnalyticsNodes(ctx)` returning the spec's twelve nodes, wired to the plugin's `nodes(ctx)`, reconciled by the CLI's engine against `state/<env>.analytics.json` - with the site's `state/<env>.json` and the site's node set provably untouched
**Pointers:** `packages/analytics/src/nodes.ts` (tasks 48–51 and 53 - the twelve node factories being assembled), `packages/analytics/src/commands.ts` (task 47 - the module holding the plugin's own command bodies), `packages/analytics/src/commands.test.ts` (new - the graph-shape and state-isolation tests), `packages/analytics/src/plugin.ts` (task 47 - where `nodes(ctx)` is wired), `packages/cli/src/plugin-commands.ts` (task 16 - the generic `bootstrap`/`destroy` verbs and the precedence comment), `packages/cli/src/plugin-commands.test.ts` (task 16 - the engine-level state-key assertions this task extends to a twelve-node plugin), `packages/cli/src/graph.ts:18-55` (`topoSort`, its unknown-dependency error at `:29` and its cycle error at `:53`), `packages/cli/src/graph.ts:58-86` (`applyGraph` and the `create ${node.title}` line at `:70` that carries the region pin into bootstrap output), `packages/cli/src/graph.ts:89-100` (`destroyGraph`), `packages/cli/src/commands.ts:54-57` (`destroy` and its `refusing to destroy "<env>" without --yes` message), `packages/cli/src/nodes.ts:1053-1087` (`buildNodes` - the site's node set, which must not gain a plugin node), `packages/core/src/state.ts:17` (`stateKey` and task 04's scope argument producing `state/<env>.analytics.json`), [the implementation note stating the node ordering](../../../changes/merged/2026-07-26-analytics_plugin.md) (Implementation notes, Stage 3 item 9)

## Steps

- [x] Write `buildAnalyticsNodes(ctx)` in `packages/analytics/src/nodes.ts` returning the twelve node factories in the spec's table order, and wire it to `nodes(ctx)` on the plugin object from task 47 so the CLI's generic verbs receive exactly this set.
- [x] Give every node a `title` that names its resource and states the `us-east-1` pin, so the region divergence appears in the `create ${node.title}` lines `applyGraph` emits at `packages/cli/src/graph.ts:70` rather than being derived silently.
- [x] Write `packages/analytics/src/commands.test.ts` asserting the graph's shape without a second engine: the twelve ids, the dependency edges the spec's ordering states (table-bucket → namespace → table → catalog-integration; salt-secret → transform-role → transform-function; error-bucket → firehose-role → firehose-stream, with firehose-role also after table and transform-function because its policy names their ARNs, and the stream also after table, catalog-integration and transform-function; log-destination → log-delivery), that every `dependsOn` entry names a node in the set, and that the edges form no cycle.
- [x] Add the state-isolation test on the plugin side: reconcile the node set against a `StateStore` from `blogwright-core` scoped to `analytics` over a recording S3 client, and assert the only state key written is `state/<env>.analytics.json` and that `state/<env>.json` is never written during either a bootstrap or a teardown.
- [x] Extend task 16's `packages/cli/src/plugin-commands.test.ts` with the engine-level half: `<plugin> bootstrap` and `<plugin> destroy --yes` over a twelve-node plugin run through `applyGraph`/`destroyGraph`, the refusal without `--yes` matches `packages/cli/src/commands.ts:56`'s shape and deletes nothing, and `buildNodes(ctx)` with the plugin installed still returns only the site's ids.
- [x] Assert the bootstrap output carries the region statement by capturing the logger lines emitted while reconciling the set and matching `us-east-1` against the node titles, rather than asserting on a line a plugin-declared `bootstrap` would have printed.
- [x] Confirm no second engine entered the package. **The step's original check - `grep -rn "topoSort\|applyGraph\|destroyGraph" packages/analytics/src/` returns nothing - was REPLACED, not satisfied.** It returns 49 matches on the final tree and returned 30+ before this task began, so it never could have returned none; the two checks that replace it are below, and both return nothing. This box is ticked for the replacements, never for the literal line above it.

  **The check as written cannot pass, and was already failing before this task started; it was replaced.** That grep matches its own documentation: `nodes.ts`'s module comment names all three functions in order to say that the CLI's engine is what walks these nodes, and every node that reasons about ordering cites `topoSort` by name. It returned 30+ matches at the tip before any of this task's edits, and 49 after them - every one a comment, none a definition. A check that reddens on correct prose is not a check.

  The property the step reaches for is *no second engine implementation entered the package*, and it has two halves, both of which held before this task and hold now:

  - `grep -rnE "(function|const) (topoSort|applyGraph|destroyGraph)" packages/analytics/src/` returns nothing - nothing here redefines any of the three.
  - `grep -rn "from 'blogwright'" packages/analytics/src/` returns nothing - no file imports the CLI, which is the only place the engine lives. The trailing quote is load-bearing: the naive `from 'blogwright` matches all 29 legitimate `blogwright-core` imports too.

  Both were run on the final tree and both return nothing. `commands.test.ts` does carry two test-local drivers (`reconcile`, `tearDown`); they restate `applyGraph`/`destroyGraph`'s *contract* over an array a test already holds, compute no ordering, live in a test file, and are named so that neither grep above could mistake one for an engine.

## Definition of done

- [x] `buildAnalyticsNodes(ctx)` returns exactly the twelve nodes the spec's table lists and is wired to the plugin's `nodes(ctx)`; a test asserts the dependency edges match the spec's stated ordering (table-bucket → namespace → table → catalog-integration; salt-secret → transform-role → transform-function; error-bucket → firehose-role → firehose-stream, with firehose-role also after table and transform-function because its policy names their ARNs, and the stream also after table, catalog-integration and transform-function; log-destination → log-delivery), that no `dependsOn` names an unknown node, and that the edges contain no cycle.
- [x] `analytics bootstrap` and `analytics destroy --yes` reconcile that node set with the same engine as the site's graph - no second `topoSort`/`applyGraph`/`destroyGraph` exists in `packages/analytics` - against the plugin's scoped state store keyed `state/<env>.analytics.json`, and a test asserts the site's `state/<env>.json` is never written during either; the `us-east-1` pinning appears in the captured bootstrap output, carried by the node titles the generic verb prints under task 16's precedence rather than by a plugin-declared `bootstrap`.
- [x] Negative space: `analytics destroy` without `--yes` refuses with the same error shape as `packages/cli/src/commands.ts:56` and deletes nothing - asserted where the verb lives (task 16's `packages/cli/src/plugin-commands.test.ts`), since the plugin declares no `destroy` of its own, and a test asserts the plugin's command table cannot shadow it.
- [x] `buildNodes` in the CLI still returns only the site's nodes with the analytics plugin installed, so `blogwright bootstrap` never provisions any of the plugin's twelve resources - asserted on the returned node ids and on the state keys touched - and `blogwright destroy --yes` removes none of them either, because task 16's guard refuses while `state/<env>.analytics.json` exists; a test drives that path with the twelve-node plugin and asserts the refusal names `blogwright analytics destroy --yes` and issues no delete.
- [x] Meets the repo definition of done (see plan.md baseline).
- [x] Reviewable: run `pnpm --filter blogwright-analytics exec vitest run commands --reporter=verbose` inside `packages/analytics` and `pnpm --filter blogwright exec vitest run plugin-commands --reporter=verbose` inside `packages/cli`; confirm the ordering test names all twelve ids explicitly, that the recording S3 client shows only `state/<env>.analytics.json` for a plugin bootstrap, and that the captured bootstrap lines contain `us-east-1`.
