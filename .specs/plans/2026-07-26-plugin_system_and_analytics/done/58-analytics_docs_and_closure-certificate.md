# Done Certificate - Task 58: Document the analytics plugin, update the toolchain and ports tables, and close the change spec

**Task:** [58-analytics_docs_and_closure.md](58-analytics_docs_and_closure.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-08-31

> This certificate is a verification protocol for Task 58. A validating agent discharges it:
> for each obligation, collect the named evidence, run the named checks, set the Status, then
> derive the Conclusion by the rubric below. Do not mark an obligation SATISFIED without its
> evidence; do not record DONE with any non-SATISFIED obligation.

## Definition

DONE(Task 58) ≡ every obligation O1…O6 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) - not by assertion.

## Premises

- **P1 - Goal.** DEVELOPMENT.md records Vite/SvelteKit and `AnalyticsQuery` and no longer counts the workspace at four packages, `packages/analytics/README.md` documents install, the five steady-state actions, the region pinning and the privacy contract, a changeset states the semver impact, the spec's two remaining open questions are each resolved or owned, and the plugin-system spec - whose flip task 20 deferred here - is merged, leaving the pds and analytics specs pending for tasks 60 and 61.
- **P2 - Obligations.** The task is done iff O1…O6 all hold. One Oi per definition-of-done item, in DoD order; O6 is the `Reviewable:` item.
- **P3 - Invariants.** Must not break the existing rows of DEVELOPMENT.md's Toolchain and ports tables (`DEVELOPMENT.md:12-22`, `:72-81`), the merged-spec links in `.specs/README.md`'s Change specs section, the relative links inside the moved change spec (which point at `../../packages/…` and `../../DEVELOPMENT.md` and gain a directory level under `merged/`), or the changeset fixed group at `.changeset/config.json:5`. Must not move or flip the pds or analytics specs: tasks 60 and 61 do that.

## Obligations

- **O1 - The two DEVELOPMENT.md table rows, and the two workspace counts a fifth package invalidates.**
  - *Claim:* the Toolchain table carries the Vite/SvelteKit row naming the dashboard build, the ports table carries `AnalyticsQuery` with its defining module, real adapter and test fake, and neither the pnpm toolchain row nor the four-package-split Assumption still says four.
  - *Evidence to collect:* read `DEVELOPMENT.md:12-23` and confirm a Vite/SvelteKit row exists with a version channel and a note scoping it to `packages/analytics/app` → `dist/app`; read `DEVELOPMENT.md:72-82` and confirm the `AnalyticsQuery` row names `analytics/src/ports.ts`, `createDuckDbAnalyticsQuery` (`analytics/src/adapters/duckdb-query.ts`) and the fixture-backed fake, in four columns like the `Vcs` row; run `grep -nE "four packages|four-package" DEVELOPMENT.md` and expect no hits; read the amended Assumption and confirm it names `blogwright-analytics` beside `blogwright-pds`.
  - *Checks:* resolve each path named in the new ports row against the working tree - confirm `packages/analytics/src/ports.ts` and `packages/analytics/src/adapters/duckdb-query.ts` exist and export what the row claims, so the table does not document a file that is not there. The two counts fail silently: `pnpm-workspace.yaml`'s `packages/*` glob picks the new package up with no edit, so nothing in CI notices either statement going stale - the grep is the only gate.
  - *Status:* ☑ **SATISFIED.** `DEVELOPMENT.md:22` carries `| Vite / SvelteKit | 8 / 2 (Svelte 5) | builds the analytics dashboard only (packages/analytics/app → dist/app); static adapter, no server |`, below `rolldown` (`:21`) and above `jj` (`:23`); the channel resolves against `packages/analytics/package.json` (`vite ^8.2.2`, `@sveltejs/kit ^2.70.3`, `svelte ^5.57.0`, `@sveltejs/adapter-static ^3.0.10`) and the static/`../dist/app` claim against `packages/analytics/app/svelte.config.js`. `DEVELOPMENT.md:85` carries the `AnalyticsQuery` row in the same four columns as `Vcs` (`:80`); every row in `:74-85` has four columns and the pre-existing rows are byte-identical. Each path the row names resolves and exports what it claims: `packages/analytics/src/ports.ts:66` `export interface AnalyticsQuery`, `packages/analytics/src/adapters/duckdb-query.ts:378` `export function createDuckDbAnalyticsQuery`, `packages/analytics/src/fixture-query.ts:71` `export function createFixtureAnalyticsQuery`. `grep -n "four packages\|four-package" DEVELOPMENT.md` exits 1 with no output. **Falsified before being reported green:** appending `workspace of four packages under packages/` to the file made the grep match at `:399` and exit 0; restoring left the tree byte-identical to `675febf5`. Both counts are genuinely corrected rather than reworded around the grep - `:16` now reads "workspace of five packages", and the Assumption (`:343-346`) enumerates `core / cli / pds / analytics / build-agent` and names `blogwright-analytics` beside `blogwright-pds` as the second worked instance.

- **O2 - The package README and the in-code documentation.**
  - *Claim:* `packages/analytics/README.md` documents install, the five steady-state actions, the us-east-1 pinning and the privacy contract - with the optional `backfill` entry deliberately absent until task 61 - and every public export carries a doc comment while every module opens with an ownership comment.
  - *Evidence to collect:* read `packages/analytics/README.md` and confirm it names `blogwright plugin add analytics`, the five steady-state actions (`init`, `bootstrap`, `status`, `dashboard`, `destroy --yes`), the us-east-1 pinning with its CloudFront reason, and the two privacy statements (the raw viewer IP is never stored; `cs(Cookie)` and `x-forwarded-for` are never selected); confirm no `backfill` entry documents an action whose body still raises its not-yet-available error; read the exports of `packages/analytics/src/index.ts` and confirm each has a doc comment, and read the first line of every module under `packages/analytics/src/` and `packages/analytics/src/transform/` for the ownership comment.
  - *Checks:* resolve the privacy claim against the code - confirm `packages/analytics/src/schema.ts`'s field selection (task 39) contains neither `cs(Cookie)` nor `x-forwarded-for`, and that no column in the row the transform emits holds the raw IP, so the README documents the behaviour rather than an intention.
  - *Status:* ☑ **SATISFIED, with one recorded non-blocking defect.** `packages/analytics/README.md` names `blogwright plugin add analytics` (§Install), the five steady-state actions in a table (`init`, `bootstrap`, `status`, `dashboard`, `destroy [env] --yes`), the us-east-1 pinning with the CloudFront reason (§Everything is created in us-east-1), and both privacy statements (§Privacy). `grep -in backfill packages/analytics/README.md` exits 1 - no entry - while `packages/analytics/src/commands.ts:391` still calls `pendingAction('backfill', 61)`, which throws `blogwright analytics backfill is not implemented yet - task 61 lands this command`, so the absence is the required one. Every factual claim in the README resolved against code, not accepted from the spec: the twelve resources `bootstrap` enumerates are exactly `buildAnalyticsNodes`' twelve (`nodes.ts:2984-3003`); the eight named queries are `ANALYTICS_QUERIES`' seven charted keys plus `[ROW_COUNT_QUERY]` (`queries.ts:223-383`), reached through `queryDefinition` (`:451`) which is the server's only lookup (`server.ts:480`); the six `Configuration` defaults match `config.ts:159,162,165,172,266,274`; the `destroy` refusal matches `packages/cli/src/commands.ts:110` and the `plugin remove` teardown offer `packages/cli/src/plugin-commands.ts:752,1170`. **Privacy check, resolved against `schema.ts`:** `CLOUDFRONT_RECORD_FIELDS` (`:181-186`) is the sixteen `FIELD_TO_COLUMN` keys plus `DERIVATION_ONLY_FIELDS`; neither `cs(Cookie)` nor `x-forwarded-for` appears anywhere in the selection, and `c-ip` (`VIEWER_IP_FIELD`, `:115`) has no `FIELD_TO_COLUMN` entry, so no column holds it - `transform/map-record.ts:336-341` reads it only into `visitorKey`. `visitor_key` is `createHash('sha256')` over IP‖UA‖salt with the salt last (`transform/visitor-key.ts:131-133`) and the salt is `createHmac('sha256', secret).update(day)` (`:93`), exactly as the README states. In-code documentation: a mechanical sweep of every non-test module under `packages/analytics/src/` found no public export without a doc comment and no module without an ownership comment - the five mechanical misses all resolve (the four `export *` lines share one block; `plugin.ts:263`'s default re-exports the const documented at `:230-236`; `transform/rolldown.config.ts` is a build config, not a public export, and carries a 30-line ownership comment after its imports; the four `aws/*.ts` modules place their ownership comment after the import block, verified present and substantial at `firehose.ts:10`, `glue.ts:5`, `lambda.ts:5`, `s3tables.ts:5`). **Defect, non-blocking, recorded rather than waived:** two of the three doc comments this task's sweep *added* to `packages/analytics/src/aws/s3tables.ts` make claims the code does not support. `:77-79` says `metadataLocation` "is what tells the `analytics-table` node whether the Iceberg table has data behind it yet" - but `metadataLocation` appears nowhere outside `s3tables.ts` and its own tests, and the `analytics-table` node (`nodes.ts:600-620`) reads only `table.name` and `table.arn`, deciding presence on `table === undefined`. `:55-58` says `arn` and `name` are "the two fields the `analytics-table-bucket` node reconciles against" - but that node (`nodes.ts:601-614` equivalent, the bucket node) reads neither: it checks `bucket === undefined` and then calls `recordTableBucket` (`:379-383`), which derives both values from config. This is the same failure mode as the `handler.ts` finding this task was routed to fix - a comment naming a consumer that does not consume - introduced by the sweep that existed to remove it. No behavioural surface; two phrases to correct.

- **O3 - The two remaining open questions and no bare TODO.**
  - *Claim:* table record expiration (in its corrected 2026-07-27 form - no row retention for self-created S3 tables, so row expiry would be whole-`day`-partition deletes the plugin issues itself) and the Glue integration's adopt-and-never-delete contract are each resolved in the change description or recorded as out of scope with an owner, with pointers for the settled backfill (task 61) and salt-cadence (tasks 41/45) decisions; and no bare `// TODO` exists in the new code.
  - *Evidence to collect:* count the `- ` items under §Open questions in `.specs/changes/merged/2026-07-26-analytics_plugin.md` - expect two, and expect one entry in the change description per item; confirm the Glue entry points at `packages/analytics/src/nodes.ts` (task 49) and that the settled-decision pointers name task 61 (backfill) and tasks 41/45 (the cadence, recorded in `packages/analytics/src/transform/visitor-key.ts` and the query semantics); run `grep -rn "TODO" packages/analytics/` - expect either no output or every match carrying an owner and a tracking reference, per `DEVELOPMENT.md:251`.
  - *Status:* ☑ **SATISFIED, against the corrected check.** `.specs/changes/merged/2026-07-26-analytics_plugin.md` §Open questions (`:755-775`) holds exactly two `- ` items - row expiration and the Glue contract - and each has an entry in the change description: D3 (out of scope, owner the spec's owner, in the corrected 2026-07-27 form, which matches the spec's own text) and D4 (resolved at task 49). D4's every clause resolves against `packages/analytics/src/nodes.ts:598-652`: `read()` adopts after verifying the source, `create()` re-reads rather than trusting the `read()` before it and raises naming the source when the federation is still unreadable, and `delete()` is an inert body whose comment gives the staging-breaks-production reason and cites core's account-global OIDC precedent; `GlueClient` exposes only `createCatalogFederation` and `getCatalogFederation` (`aws/glue.ts:225,261`), so the structural half of the guard holds. The two pointers resolve: backfill at task 61 (`commands.ts:391`, declared at `plugin.ts:256`) and the daily-salt cadence at `transform/visitor-key.ts` and `queries.ts:208-221`. **The TODO check as written cannot pass, and the correction is verified in both directions.** `grep -rn "TODO" packages/analytics/` returns **7** on this tree, all seven under `packages/analytics/app/.svelte-kit/output/`, which `.gitignore:5` excludes and which only exists after a build; zero matches lie outside it. `git grep -n "TODO" -- packages/analytics/` exits **128** here (`fatal: not a git repository` - `.git` lives in the main repo, this is a non-colocated jj workspace), confirming the implementer's report. The replacement, `jj file list packages/analytics | xargs grep -n "TODO"`, produces no output and exits 1 over 65 tracked files. **Falsified before being reported green:** appending `// TODO` to `packages/analytics/src/index.ts` made it match at `:39` and exit 0; restoring left the tree byte-identical to `675febf5`. The original check and the reason are recorded beside the DoD line rather than substituted silently.

- **O4 - The changeset and the one executed merge plan.**
  - *Claim:* a changeset states the semver impact, and the merge plan of the one spec this task owns is executed - the plugin-system spec's, deferred here by task 20 - leaving exactly two pending entries: the pds spec named as task 60's and the analytics spec as task 61's, with the analytics deferral (§Backfill of historical logs) recorded in the change description.
  - *Evidence to collect:* read the new file under `.changeset/` and confirm it declares the new `blogwright-analytics` package and a minor on `blogwright-core`, naming `signingUsEast1`, the transport seam and the `LogsClient` delivery parameters as the reason - the plugin's four service clients live in `blogwright-analytics` and are not a core change; read the `Status:` header of `.specs/changes/merged/2026-07-26-cli_plugin_system.md` and confirm it says `Merged` with a `Merged:` date; run `ls .specs/changes/` and expect `merged/` plus `2026-07-26-migrate_pds_to_plugin_system.md` and `2026-07-26-analytics_plugin.md` and nothing else; read `.specs/README.md`'s Change specs section and confirm the merged list carries the plugin-system spec and the pending section holds exactly two entries naming tasks 60 and 61.
  - *Checks:* resolve every relative link inside the moved spec and inside `.specs/README.md` - the move adds a directory level, so links such as `../../packages/cli/src/nodes.ts` and `../../DEVELOPMENT.md` must have been re-pointed; confirm each target exists. Confirm the plugin-system flip was earned, not assumed: `SendOptions.service` accepts a plugin-supplied descriptor (task 31) and `AwsClients` carries `signingUsEast1` (task 38); if either is absent the obligation is UNSATISFIED even when the header says `Merged`. A flipped analytics or pds header here is a defect, not an over-delivery - each has an outstanding `Proposed changes` block.
  - *Status:* ☒ **UNSATISFIED - half met, and the unmet half is correctly refused rather than forced.** The changeset half holds: `.changeset/analytics-plugin-package.md` declares `blogwright-analytics: minor` and `blogwright-core: minor`, names `signingUsEast1`, the `SendOptions.service` descriptor seam and `putDeliveryDestination`'s `outputFormat` / `createDelivery`'s `recordFields` and `fieldDelimiter` as the core reason, and states that the plugin's four service clients live in `blogwright-analytics` with `SIGNING_NAMES` unchanged - all true (`packages/core/src/clients.ts:33`, `packages/core/src/aws/endpoint.ts:35-43`, `packages/analytics/src/aws/*.ts`). `blogwright-analytics` is in the fixed group at `.changeset/config.json:5`. The pds and analytics specs are correctly not flipped and their pending entries name tasks 60 and 61. **The flip half is not met.** `.specs/changes/merged/2026-07-26-cli_plugin_system.md` does not exist; the spec is still at `.specs/changes/2026-07-26-cli_plugin_system.md` with `**Status:** Proposed` (`:3`); `ls .specs/changes/` lists `merged/` plus **three** spec files; `.specs/README.md`'s pending section holds **three** entries. **The refusal is adjudicated CORRECT, and is not a shortfall of execution.** The seam the DoD conditions the flip on is genuinely present (`endpoint.ts:39-43` `ServiceDescriptor`, `clients.ts:33` `signingUsEast1`), but the same spec's §Plugin SPI → *A plugin owns its own topography* (`:226`, invariant at `:229-232`) states without qualification that "no config key of a plugin's is read by a site node", and `packages/cli/src/nodes.ts:971` still branches on `ctx.config.pds` inside `oidcRolePolicyStatements` (`:921`, the `<env>-deploy` inline policy applied at `:1032`), interpolating `ctx.config.pds.secretName` at `:995`, with the code's own comment at `:985-987` saying task 59 deletes both. The spec's Decisions block (`:793-802`) names *this exact code* as work that "moves into the pds plugin", and the pds plugin already attaches its own named inline policy (`packages/pds/src/nodes.ts:49,120`), so the site statement is the duplication the spec forbids. No clause of the spec admits it. The ROUTED CONSTRAINT in the task file (`:56-71`, pre-existing - this diff begins at `@@ -72`) instructs exactly this outcome: "if it still stands, defer the flip again and record why, exactly as task 20 did." Flipping would have stamped `Merged` on a spec whose own invariant the code visibly violates. **Marked UNSATISFIED because the obligation is objectively unmet, not because the disposition was wrong.**

- **O5 - Meets the repo definition of done.**
  - *Claim:* tests written with the change pass, the lint/format/dead-code gates are clean, and limits are named constants or validated config fields.
  - *Evidence to collect:* run `pnpm build`, `pnpm test`, `pnpm lint`, `pnpm exec oxfmt --check .`, and `pnpm knip` from the repo root - expect all clean; confirm a changeset exists (`.changeset/*.md`) if the change is user-facing.
  - *Status:* ☑ **SATISFIED.** All six CI gates run from the workspace root in `.github/workflows/ci.yml` order (`:21-29`) exit **0**: `pnpm build` 0, `pnpm typecheck` 0, `pnpm test` 0, `pnpm lint` 0, `pnpm exec oxfmt --check .` 0, `pnpm knip` 0. Test counts are exactly the tip baselines, as a prose-and-comments change must leave them: `blogwright-analytics` 764, `blogwright` 376, `blogwright-core` 149 passed + 1 skipped, `blogwright-pds` 150, `blogwright-build-agent` 27. Structurally guaranteed as well as observed - no `*.test.ts` is in the diff and no executable line changed. The changeset exists.

- **O6 - Run the five gates, then confirm the plugin-system spec moved and every link resolves (Reviewable).**
  - *Claim:* a reviewer can run `pnpm build && pnpm test && pnpm lint && pnpm exec oxfmt --check . && pnpm knip` and observe clean gates, `.specs/changes/` holding `merged/`, the pds spec and the analytics spec and nothing else, the merged plugin-system spec carrying a `Merged:` date, and every link in `.specs/README.md` resolving.
  - *Evidence to collect:* run the gate command line and record the output; run `ls .specs/changes/` and expect `merged` plus the two pending spec files; run `grep -o "](\([^)]*\))" .specs/README.md` and check each target path exists on disk.
  - *Status:* ☒ **UNSATISFIED - the gate chain passes, two of the three post-gate confirmations do not, both as consequences of O4.** The `Reviewable:` line was run verbatim: `pnpm build && pnpm test && pnpm lint && pnpm exec oxfmt --check . && pnpm knip` exits **0**. Every link in `.specs/README.md` resolves - all 11 targets exist on disk, the check this build has broken once. But `ls .specs/changes/` lists `merged/` plus **three** spec files rather than two, and `.specs/changes/merged/2026-07-26-cli_plugin_system.md` does not exist, so it carries no `Merged:` date. Both deviations are recorded verbatim beside the DoD line rather than worked around, and both follow from the refusal adjudicated under O4.

## Regression check

For each module the task touched, the validator traces one downstream caller:

- `.specs/README.md`'s Change specs section is the index readers follow to reach every spec → expect the moved plugin-system spec to be reachable at its new depth, and the pds and analytics pending links still to resolve at the old one : ☑ **PRESERVED.** The move did not happen, so all three specs are reachable at the old depth; all 11 links in `.specs/README.md` resolve. The Change specs section was rewritten and every entry now names the task that flips it, with the plugin-system and pds entries stating plainly that task 59 is held for a later release and task 60 is blocked behind it rather than unstarted - corroborated by `blocked/59-*`, `blocked/60-*` and the `parked/task-59` jj bookmark.
- `.specs/changes/merged/2026-07-26-cli_plugin_system.md` relative links (`../../packages/…`, `../../DEVELOPMENT.md`, the two companion specs - still unmoved, so they need a `../` back out of `merged/`) after the move → expect each to resolve from its new directory : ☑ **PRESERVED (vacuously - the move was refused).** No link needed re-pointing and none was touched; all 22 relative links in `.specs/changes/2026-07-26-cli_plugin_system.md` resolve from its current directory. The re-pointing obligation transfers intact to whichever task performs the flip.
- `.changeset/config.json:5` fixed group with the new package's changeset present → expect `pnpm changeset:version` to version `blogwright-analytics` as intended and not to break the existing three-package lockstep : ☑ **PRESERVED.** `.changeset/config.json:5` is unmodified by this diff and reads `[["blogwright", "blogwright-core", "blogwright-pds", "blogwright-analytics"]]`; `blogwright-analytics` is 0.3.3, not private, and is not in `ignore`. The new changeset bumps two members of that group, which the lockstep already carries. Note only: its second paragraph restates core surfaces already covered by `.changeset/core-signing-us-east-1.md`, `core-transport-service-descriptor.md` and `logs-delivery-output-format.md`, so `blogwright-core`'s changelog will carry those facts twice at release. The DoD asked for it explicitly; harmless to the version computation.
- `DEVELOPMENT.md:72-81` ports table consumers (tasks 05, 06 and 20 add rows to the same table) → expect the existing rows to be intact and the table to render : ☑ **PRESERVED.** Every row in `DEVELOPMENT.md:74-85` carries four columns; the ten pre-existing rows are byte-identical in the diff and the `AnalyticsQuery` row is appended in the `Vcs`/`PingBuilder` shape. The Toolchain table (`:12-23`) likewise: three columns throughout, the new row inserted between `rolldown` and `jj`, no existing row disturbed except the intended `pnpm` count.

## Residue

Notes for the validator: merge-plan steps 1 and 2 (folding the `Proposed changes` blocks and the `AnalyticsConfig` / `PageView` `$defs` into canonical pages) cannot be executed while no canonical spec set exists - confirm the change description says so rather than silently skipping them. The Starlight docs site under `docs/` is not in this task's scope, so the public documentation of the analytics surface at `blogwright.iamstan.dev` remains a follow-up. Whether the repo `README.md`'s Commands block should name `blogwright analytics …` alongside `pds` is left to task 20's plugin-surface documentation. The pds and analytics specs are deliberately left pending: the pds spec's last blocks land at tasks 59 and 60 (the flip at 60), and the analytics spec's §Backfill of historical logs lands at task 61 (the flip with it) - the same reason task 20 deferred the plugin-system spec's flip here. Shrinking the pending list below two entries in this task would merge a spec with an outstanding `Proposed changes` block - mark UNSATISFIED if either entry is gone.

## Conclusion

<!-- Validator derives this from the obligation statuses and the regression check, per the rubric. -->
VERDICT: **PARTIAL**
CONFIDENCE: **high**
SUMMARY: O1, O2, O3 and O5 are SATISFIED on verified evidence and every gate is clean, but O4 and O6 are UNSATISFIED because the plugin-system spec's `Status:` is still `Proposed` and `.specs/changes/` holds three pending specs - a refusal this validator independently adjudicates **correct** (the spec's own topography invariant is violated by `packages/cli/src/nodes.ts:971`, which the spec's Decisions block names as work that must move), so the residue is a known plan defect rather than incomplete work, and the certificate's own rubric forbids DONE with a non-SATISFIED obligation.

**Why PARTIAL rather than DONE against a corrected definition of done.** The case for DONE is real: `plan.md:725-742` carries this exact outcome as a standing prediction naming task 58, and names the fix (one line in this task's DoD conditioning the flip on task 59 as well as the seam, or an edge from 59) that was never applied. Everything task 58 controls is done and correct. PARTIAL is chosen anyway for one reason that is not bookkeeping: **the flip currently has no owner whose contract can carry it.** D5 assigns it to task 60, but task 60's definition of done (`blocked/60-cli_bootstrap_plugin_state_warning.md:30`) flips only the *pds* spec and asserts that `.specs/README.md`'s pending list then holds "exactly one entry - the analytics spec". That is arithmetically incompatible with the plugin-system spec still being pending: task 60 as written must either fail its own DoD on a count of two, or shrink the list by dropping a spec it was never told to flip. Marking task 58 DONE would retire the last task whose contract mentions this flip and leave it living only in prose - the precise "recorded-but-unowned finding gets lost" failure the routed-finding preamble exists to prevent. PARTIAL keeps task 58 in the same parked cohort as 59 and 60, which share its root cause and should clear with it.

**What would make it DONE, with no change to this tree.** Two plan edits: (1) amend this task's DoD item 5 and this certificate's O4/O6 to condition the plugin-system flip on the topography obligation as well as the seam - the fix `plan.md:740-742` already proposed; and (2) amend task 60's DoD line 30 to flip **both** specs, expect exactly one pending entry, and carry the check `grep -n "config\.pds" packages/cli/src/nodes.ts` returning nothing. With those two lines every obligation above is satisfied by the tree exactly as it stands. The residue is a plan defect measured in lines of markdown, not in work.
