# Done Certificate - Task 30: Ship the pds changeset and record the plugin manifest in DEVELOPMENT.md

**Task:** [30-pds_migration_closure.md](30-pds_migration_closure.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-08-31

> This certificate is a verification protocol for Task 30. A validating agent discharges it:
> for each obligation, collect the named evidence, run the named checks, set the Status, then
> derive the Conclusion by the rubric below. Do not mark an obligation SATISFIED without its
> evidence; do not record DONE with any non-SATISFIED obligation.

## Definition

DONE(Task 30) ≡ every obligation O1…O6 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) - not by assertion.

## Premises

- **P1 - Goal.** The migration ships with a changeset stating its semver impact and carrying the spec's five operator-visible changes, `DEVELOPMENT.md` records the `blogwright.plugin` manifest as the feature-package mechanism, and the change spec's `Status:` flip is deliberately deferred to task 60, its two outstanding blocks landing at tasks 59 and 60.
- **P2 - Obligations.** The task is done iff O1…O6 all hold. One Oi per definition-of-done item, in DoD order; O6 is the `Reviewable:` item.
- **P3 - Invariants.** Must not break the release pipeline's changeset consumption (`.changeset/config.json`), the link targets in `.specs/README.md`, or `DEVELOPMENT.md`'s existing §Hexagonal architecture content beyond the one paragraph being amended. Must not move or flip the pds change spec: task 60 does that.

## Obligations

- **O1 - The changeset names three packages, their bumps, and the unchanged file shape.**
  - *Claim:* a `.changeset/*.md` file names `blogwright-core`, `blogwright-pds` and `blogwright` with intended semver bumps and states that the on-disk config file shape is unchanged - only where the pds default and validation live moved.
  - *Evidence to collect:* read the new `.changeset/*.md` front matter and body; run `pnpm changeset status` from the repo root and record the three packages and their bump levels; confirm the core bump reflects the removed validation and the now-optional `PdsConfig.secretName` (task 27's O4).
  - *Evidence collected 2026-08-31:* `.changeset/pds-migrated-onto-the-plugin-system.md` front matter is `"blogwright-core": minor` / `"blogwright-pds": minor` / `"blogwright": minor`. The body's second paragraph carries **"Nothing on disk changes."** and states the move explicitly - out of core's `parseConfig`/`validateConfig`, into `blogwright-pds`'s `resolvePdsSecretName`/`validatePdsConfig`. `PdsConfig.secretName` resolves as `string | undefined` at `packages/core/src/config.ts:57`, and `packages/core/src/config.ts` retains no pds validation or defaulting (only the type declaration and `OpsConfig.pds` at `:130`), so the core bump's stated rationale holds. `Names.githubRole` is real (`packages/core/src/config.ts:369,397`, `${env}-${siteName}-gh`, pinned at `config.test.ts:232-233`).
    `pnpm changeset status` **cannot run from this jj workspace** - `/Users/ant/code/blogwright-task-30` is non-colocated, has no `.git`, and the command exits 1 in `@changesets/git`'s `getDivergedCommit`. This is a harness property, NOT a defect in the Reviewable line: the colocated main tree `/Users/ant/code/blogwright` has a `.git` with `main`, and `pnpm changeset status` there exits **0**. The check is therefore dischargeable at merge time. The validator discharged its substance here by running the **real** `@changesets/cli` binary against a faithful reconstruction of this tree's `.changeset/` plus every workspace `package.json` in a scratch git repo: *"Packages to be bumped at minor: blogwright-core, blogwright-pds, blogwright, blogwright-analytics"* (the fourth rides `.changeset/config.json`'s `fixed` group). Isolating this changeset alone in a second scratch repo produced the same four, in the front matter's own order. Independently, `@changesets/read` → `@changesets/assemble-release-plan` over `@manypkg/get-packages` reads 19 changesets, parses this one as `[{blogwright-core, minor}, {blogwright-pds, minor}, {blogwright, minor}]`, and plans all four at `0.3.3 -> 0.4.0`.
  - *Status:* ☒ **SATISFIED**

- **O2 - `DEVELOPMENT.md` names the manifest field and its example still reads true.**
  - *Claim:* the "Features live in their own packages" paragraph at `DEVELOPMENT.md:100-103` names the `blogwright.plugin` manifest field as the mechanism, and its `PdsContext`/`OpsContext` sentence remains accurate after task 24's narrowing.
  - *Evidence to collect:* read `DEVELOPMENT.md` §Hexagonal architecture and capture the amended paragraph; read `packages/pds/src/context.ts` and confirm the sentence "satisfied structurally by the CLI's `OpsContext`" still describes the type as written.
  - *Evidence collected 2026-08-31:* the amended bullet is `DEVELOPMENT.md:102-117` and names `"blogwright": { "plugin": "<namespace>" }` as the mechanism, what discovery scans for, what the default export declares, and that `cli.ts` carries no branch for a feature package - confirmed by `grep -ic pds packages/cli/src/cli.ts` = **0**. `packages/pds/src/context.ts:49-55` declares `PdsContext` as `Pick<PluginContext<PdsConfig>, 'env'|'domain'|'config'|'ports'|'logger'|'tags'>` with `clients` narrowed, so the amended `Pick` wording resolves against the code.
    **Mutation reproduced from scratch.** Digest before: `jj diff --git | shasum` = `543c7cf85263f0414ec99a68b27e5e372301978e`. Added `| 'pluginConfig'` to that `Pick`. Rebuilt `packages/pds`, then `pnpm typecheck` in `packages/cli` - **exit 2**, with exactly the two diagnostics task 24 predicted:
    `src/commands.ts(240,25): error TS2345: Argument of type 'OpsContext' is not assignable to parameter of type 'PdsContext'.` and
    `src/context.test.ts(327,11): error TS2741: Property 'pluginConfig' is missing in type 'OpsContext' but required in type 'PdsContext'.`
    Restored; digest after = `543c7cf85263f0414ec99a68b27e5e372301978e`, byte-identical. The implementer's reported blind spot is **real and confirmed**: the same mutation with `packages/pds` NOT rebuilt left `packages/cli`'s `pnpm typecheck` at exit **0**, because the CLI resolves `blogwright-pds` through its built `dist/*.d.ts`. CI's `pnpm build` before `pnpm typecheck` closes it; a bare local `pnpm typecheck` does not.
  - *Status:* ☒ **SATISFIED**

- **O3 - The five operator-visible changes are stated, and every divergence with them.**
  - *Claim:* the changeset carries the spec's §Upgrading a deployed stack list in full - `blogwright pds bootstrap` first and marked required, the three new `pds` lifecycle verbs, `blogwright destroy` refusing while `state/<env>.pds.json` exists, the shorter help section, and the built-in commands no longer rejecting a malformed `pds` block - plus whatever task 28's tests pinned beyond that and whatever help text task 29 reshaped.
  - *Evidence to collect:* read the changeset body and check off all five items; read task 28's pinned outcome (its tests and commit description) and task 29's commit description listing lost help guidance; confirm each appears; if neither task produced a further divergence, confirm the changeset says so explicitly rather than being silent.
  - *Checks:* the first item is the load-bearing one, and it is easy to soften into a suggestion. This is the release the instruction travels in: task 59 removes the site's own grant in a later one, and a stack that never ran the verb loses it at its next `blogwright bootstrap` after that release, because `applyOidcRole` rewrites the whole `<env>-deploy` document (`packages/cli/src/nodes.ts:840-842,962`). A changeset that omits it, or files it under "nice to have", is UNSATISFIED.
  - *Evidence collected 2026-08-31:* all five items are present, numbered, in the spec's order, under `## Upgrading a deployed stack` with the sentence "Five things change for an operator, and this list is complete." The **check passes**: item 1 is bold, reads "must be run once per stack", "the only step that is not optional", and the changeset opens with a standalone bold line - "run `blogwright pds bootstrap <env>` once per environment. It is required, not optional". It is written **forward-looking** about task 59 throughout ("that removal is **not** in this release"; "That warning is also not in this release") and never describes task 59 or task 60 as shipped.
    Every operative claim in item 1 resolves against code: the precondition refusal is `packages/pds/src/nodes.ts:103` (`... run \`blogwright bootstrap --env ${ctx.env}\` before \`blogwright pds bootstrap\``), and the three "contributes nothing" cases are exactly `packages/pds/src/nodes.ts:196` - `if (!pds || !ctx.config.githubRepo || ctx.env === PREVIEW_ENV) return [];`.
    **Item 3 deliberately diverges from the spec, and the changeset is the correct half.** The spec's §Upgrading item 3 names `blogwright pds destroy --yes`; the shipped refusal is built at `packages/cli/src/commands.ts:186-188` as `` `  - ${scope}: run \`blogwright ${scope} destroy ${ctx.env} --yes\` first` ``, i.e. `blogwright pds destroy production --yes`. The reason the changeset gives is verified: `packages/cli/src/plugin-commands.ts:156,701-703` resolves `values.env ?? envPositional ?? DEFAULT_ENV` with `DEFAULT_ENV = 'production'`, so an env-less plugin command silently targets production - wrong every time the guard fires from `previewTeardown`. This is the one place the five-item list is not word for word, and the changeset is a release ahead of the spec rather than divergent from the code.
    **Beyond the five**, the changeset states four further divergences and each resolves: the wrapper prefix and preserved `cause` (`packages/cli/src/plugins.ts:673-675`), the absent-block sentence (`packages/pds/src/config.ts:44`, `NO_PDS_SECTION_MESSAGE`, shared with `requirePdsConfig`) and `"pds": null` as absent - both from task 28's `.changeset/pds-config-validation-timing.md`; the unknown-action listing of **nine** actions (`packages/cli/src/cli.test.ts:1137-1158` - six declared plus three lifecycle - pinned at `:1316-1331`) and the 26→29 help/dispatch gap closed in this release (`cli.test.ts:1333-1357`) - both from task 29's `.changeset/pds-dispatched-as-a-plugin.md`; the five help notes that moved to the docs, traced verbatim to task 26's `.changeset/pds-discoverable-as-a-plugin.md:8` and spot-checked as present in `docs/src/content/docs/guides/`; and the untouched post-deploy sync. Nothing is left implied.
    The definition-of-done sentence holds: the changeset alone names the verb, marks it required, gives the form with an environment, states the precondition and the three no-op cases, and says the grant removal is not in this release.
  - *Status:* ☒ **SATISFIED**

- **O4 - Merge-plan bookkeeping is correctly incomplete, and the open questions survive.**
  - *Claim:* merge-plan step 3 is done; steps 4 and 5 are NOT - the spec's `Status:` still reads `Proposed`, the file is still at `.specs/changes/2026-07-26-migrate_pds_to_plugin_system.md`, and `.specs/README.md` still lists it as pending - with the deferral to task 60 recorded in the change description and the three unanswered questions (an `afterDeploy` hook, `OpsConfig` holding plugin blocks as an opaque map, shorter pds aliases) recorded so task 60's move carries them forward.
  - *Evidence to collect:* run `ls .specs/changes .specs/changes/merged` and confirm the file has NOT moved; read its header line and confirm `Status: Proposed`; read `.specs/README.md` and confirm the entry is present; read the change description for the deferral and the reason.
  - *Checks:* a flipped `Status:` here is a defect, not an overachievement. Two of the spec's blocks have not landed - §The site graph drops its pds branch (task 59, a release later) and §`bootstrap` warns while plugin state exists (task 60) - and merging a spec with an outstanding `Proposed changes` block is exactly the finding that split task 20 from task 58. If the file has moved, mark UNSATISFIED and name the block.
  - *Evidence collected 2026-08-31:* the **negative property holds in every particular**. `.specs/changes/` contains `2026-07-26-migrate_pds_to_plugin_system.md` - the file has NOT moved; `.specs/changes/merged/` holds only `2026-07-22-adopt_orphaned_resources_on_bootstrap.md` and `2026-07-22-persist_partial_bootstrap_state.md`. Its header line 3 still reads `**Status:** Proposed`. `.specs/README.md:34` still lists it **second of three** pending proposals, and the entry, the plugin-system entry and the analytics entry all still resolve. `jj st` shows the diff touches four files and none of them is the spec or the README.
    Merge-plan accounting is complete and correct: step 1 not-applicable (D1, owner Ant Stanley), step 2 not-applicable (D2, same owner), step 3 done (D3), steps 4-5 deferred to task 60 (D4). Independently re-derived: the spec's merge-plan step 1 reads "*once one exists*" and the spec's own §Affected spec pages first row says "*(none - no canonical page for CLI dispatch or the pds feature yet)*", so the condition is false - **the implementer's distinction from the plugin-system spec's step 1 is real**, that step carried an unconditional "if none exists, record the SPI as a new canonical page and index it" fallback which is what forced task 20 to refuse rather than record it as vacuous. Recording this one as not-applicable is the right disposition. Step 2 is written unconditionally and `find .specs -name '*.schema.json'` returns nothing, so its object does not exist - a distinct reason, correctly given its own note.
    All three open questions are carried into `plan.md`'s Open questions block and match the spec's §Assumptions and open questions verbatim in substance (`afterDeploy` hook; `OpsConfig` as an opaque map; shorter pds aliases). They also remain in the spec file itself, so task 60's move carries them regardless.
    `.changeset/` held 18 changesets at base (20 entries incl. `README.md` and `config.json`) and holds 19 at the tip; the only `.changeset/` change is the added file, and `cli-site-graph-drops-pds.md` is confirmed absent - the mechanical guarantee D4 rests on.
  - *Status:* ☒ **SATISFIED**

- **O5 - Meets the repo definition of done.**
  - *Claim:* tests written with the change pass, the lint/format/dead-code gates are clean, and limits are named constants or validated config fields.
  - *Evidence to collect:* run `pnpm build`, `pnpm test`, `pnpm lint`, `pnpm exec oxfmt --check .`, and `pnpm knip` from the repo root - expect all clean; confirm the pinned rkey vectors in `packages/pds/src/rkey.test.ts` pass; confirm the changeset from O1 exists.
  - *Evidence collected 2026-08-31:* all six CI gates re-run by the validator from the workspace root in `.github/workflows/ci.yml` order on the unmutated tree - `pnpm build` **0**, `pnpm typecheck` **0**, `pnpm test` **0**, `pnpm lint` **0**, `pnpm exec oxfmt --check .` **0**, `pnpm knip` **0**. Totals: core 149 passed / 1 skipped, build-agent 27, pds 150, analytics 734, cli 371 - **1431 passed, 1 skipped**, matching the implementer's report exactly. `packages/pds/src/rkey.test.ts` alone: 10/10. `jj st` shows exactly the four intended files and nothing else (no stray non-TypeScript artifacts; oxfmt clean confirms it independently).
  - *Status:* ☒ **SATISFIED**

- **O6 - Reviewable: `pnpm changeset status` plus the two documents (Reviewable).**
  - *Claim:* a reviewer can run `pnpm changeset status` and `pnpm test` from the repo root and read `DEVELOPMENT.md:100-103` and `.specs/README.md`, seeing three packages with bumps, the manifest field named, `blogwright pds bootstrap` as the changeset's first upgrade step, and the pds spec still listed as pending with `Status: Proposed`.
  - *Evidence to collect:* run both commands and capture the output; capture the amended `DEVELOPMENT.md` paragraph and the updated `.specs/README.md` pending list.
  - *Evidence collected 2026-08-31:* `pnpm test` exit 0 (see O5). `pnpm changeset status` is not runnable from a non-colocated jj workspace but exits 0 in the colocated main tree, and the real `@changesets/cli` run against this tree's changeset set reports the three packages at minor (see O1). The amended `DEVELOPMENT.md` paragraph names the manifest field; `.specs/README.md` still lists the pds spec as pending with `Status: Proposed`; the changeset's first upgrade step is `blogwright pds bootstrap`, marked required. Every clause of the Reviewable line checks out.
    **Note for the reviewer who re-runs this at merge:** run it from `/Users/ant/code/blogwright` (colocated), not from a `jj workspace add` workspace. An exit 1 there is the harness, not the changeset.
  - *Status:* ☒ **SATISFIED**

## Regression check

For each module the task touched, the validator traces one downstream caller:

- `.specs/README.md`'s pending list links `changes/2026-07-26-migrate_pds_to_plugin_system.md` → expect the entry still present and still resolving, since the move belongs to task 60 : ☒ **PRESERVED** (`.specs/README.md:34`, second of three; target file still at `.specs/changes/2026-07-26-migrate_pds_to_plugin_system.md`)
- `.specs/changes/2026-07-26-cli_plugin_system.md` §Affected spec pages and `2026-07-26-analytics_plugin.md` reference the migration spec by relative path → expect those links untouched and still resolving, since nothing moves in this task : ☒ **PRESERVED** (`cli_plugin_system.md:51` and `analytics_plugin.md:48` both still resolve; neither file appears in the diff)
- `DEVELOPMENT.md` §Error handling table and §Assumptions reference `blogwright-pds` as a feature package → expect both still consistent with the amended paragraph : ☒ **PRESERVED** for the two named references (`DEVELOPMENT.md:130` §Where validation lives PDS/OAuth row; `:344`, `:368` §Assumptions) - all three still read true. **One adjacent row is stale and was correctly left alone:** `DEVELOPMENT.md:132`'s CLI-arguments row still says "`parseArgs` plus explicit dispatch in `cli.ts`; unknown commands fail with usage", which task 29 falsified - an unrecognised first word now reports `no built-in command or installed plugin claims "…"` and points at `blogwright plugin list` (`packages/cli/src/cli.test.ts:1395-1398`). Pre-existing, outside this task's permitted edit under P3, recorded in the implementer's D7 - but NOT routed into any task's definition of done. See the validator's note below.

## Residue

The migration's own spec lists five operator-visible changes, so the changeset is where that list is
either confirmed or qualified - O3 is the obligation that keeps the two honest, and it is the one
most readily discharged by assertion rather than evidence. The carried-forward questions are not
idle: whether the SPI gains an `afterDeploy` hook decides whether `packages/cli/src/commands.ts:2`'s
static import stays a recorded wart or becomes a bug, and whether `OpsConfig` should hold plugin
blocks as an opaque map is a breaking change to a published type that this migration deliberately
did not make. Not covered by the DoD: whether a canonical spec page now exists for CLI dispatch, in
which case merge-plan steps 1 and 2 (folding the Proposed changes blocks and the `PdsConfig` `$def`
into it) become live rather than no-ops.

## Conclusion

<!-- Validator derives this from the obligation statuses and the regression check, per the rubric. -->
VERDICT: ☒ **DONE**
CONFIDENCE: ☒ **high**
SUMMARY: All six obligations are SATISFIED on independently collected evidence - the changeset's three-package minor front matter confirmed by a real `@changesets/cli` run and by an independent release-plan assembly, the `DEVELOPMENT.md` manifest paragraph defended by a mutation the validator reproduced from scratch and reverted to a byte-identical tree, all five operator-visible changes present with `blogwright pds bootstrap` first and required, the merge-plan bookkeeping correctly incomplete with `Status: Proposed`, the file out of `merged/` and the README entry intact, and all six CI gates green at 1431 passed / 1 skipped - with all three regression traces PRESERVED.

### Validator's notes (non-blocking, for the merge)

1. **D7's `changeset status` wording is overbroad.** "cannot run in a jj workspace at all" is true only of a **non-colocated** workspace (`jj workspace add`). The main tree is colocated and the command exits 0 there. The Reviewable line is dischargeable at merge time and is not defective; D7's own remediation sentence already points at a colocated checkout, so only the phrase needs narrowing.
2. **The falsifiability check on the substitute evidence is shallow.** Flipping `"blogwright-core": minor` to `patch` and observing the parsed front matter change is a parse echo, not a test of the release plan: under `.changeset/config.json`'s `fixed` group - and independently because `core-config-drops-pds.md` already bumps core minor - the real `changeset status` output is **invariant** under that mutation (validator confirmed). The underlying fact is right and was verified three ways; the check attached to it could not have failed informatively.
3. **D6's `docs/` gap is recorded but unowned.** The correction to its premise is right - `grep -rn 'blogwright pds bootstrap' docs/` returns nothing at this tip, and the sentence the routed finding cited belongs to `parked/task-59`'s diff of `docs/src/content/docs/guides/ci-github-oidc.md:61`, so the gap is genuinely wider than reported. The decision to decline is also right on its merits: `grep -c plugin docs/src/content/docs/reference/cli.md` = 0, the §Invocation positional table (`:28`) still lists `blogwright pds secret <action> [env]` as its own layout, and §Exit codes (`:249`) still says `1` covers an "unknown command, `preview` action, or `pds` action". But **nothing in task 58's definition of done names `docs/`**, so as recorded this finding survives the plan. The repo's own routing mechanism - a `> **ROUTED FINDING**` blockquote in the target task's file, as task 20's gate used on task 58 - was not used. Recommend routing it or amending task 58's DoD before task 30 is marked Done.
4. **The stale `DEVELOPMENT.md:132` row is recorded only in this task's decision notes** - not in plan.md's open questions, not routed to task 58. Weaker disposition than D6's; same recommendation.
5. Cosmetic: D3 gives the amended paragraph as `102-116`; it is `102-117`. The plan.md append is a pure insertion (90 `+` lines, zero `-`) landing at the end of the Open questions block, but it introduces one blank line before its first bullet, which renders the whole surrounding tight list loose. The pre-existing joined-bullet defect at `plan.md:683` was verified present at the base revision and is untouched.
