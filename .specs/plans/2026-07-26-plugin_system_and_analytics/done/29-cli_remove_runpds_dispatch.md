# Task 29 - Delete runPds and route `blogwright pds` through generic plugin dispatch

**Plan:** [plan.md](../plan.md) · **Certificate:** [29-cli_remove_runpds_dispatch-certificate.md](29-cli_remove_runpds_dispatch-certificate.md)

**Implements:** [2026-07-26-migrate_pds_to_plugin_system.md §`blogwright-cli` → Dispatch (Remove)](../../../changes/merged/2026-07-26-migrate_pds_to_plugin_system.md) and §`blogwright-cli` → Post-deploy sync (Modify) (`runPds`, `PdsValues`, the namespace import and the `command === 'pds'` branch all go here; the static `USAGE` block goes at task 26, so help is never duplicated; `deploy` keeps importing `syncAfterDeploy` directly)
**Depends on:** 10, 26
**Produces:** `blogwright pds <action>` answered by the plugin's declared commands with no pds knowledge left in `cli.ts`, multi-word `secret status`/`secret delete` declared rather than hand-shifted, and the post-deploy sync provably intact
**Pointers:** `packages/cli/src/cli.ts:4` (`import * as pds from 'blogwright-pds'`), `packages/cli/src/cli.ts:33-47` (the static pds block in `USAGE` - removed at task 26, verified absent here), `packages/cli/src/cli.ts:91` (the `identifier` flag, which stays and becomes a pass-through argument), `packages/cli/src/cli.ts:114-116` (the `command === 'pds'` branch), `packages/cli/src/cli.ts:177-184` (`PdsValues`), `packages/cli/src/cli.ts:186-232` (`runPds`, including the positional shifting at `:193-196` and the known-action set at `:197`), `packages/cli/src/commands.ts:2,97` (the `syncAfterDeploy` static import and the deploy-path call, both staying), `packages/cli/package.json:28` (`"blogwright-pds": "workspace:*"`, staying non-optional)

## Steps

- [x] Delete `runPds` (`packages/cli/src/cli.ts:186-232`), `PdsValues` (`:177-184`), the namespace import (`:4`) and the `command === 'pds'` branch (`:114-116`), letting the fall-through to task 10's generic dispatch answer the namespace.
- [x] Confirm the static pds lines are already gone from `USAGE` - task 26 removed them, so that help never listed pds twice. If they are still present, task 26 was not completed; do not re-do its work here, and do not let this task be the first commit at which help output is correct.
- [x] Keep the `identifier` flag at `packages/cli/src/cli.ts:91` and pass the parsed arguments through to the plugin command, so `--identifier` reaches the plugin's `login` without `cli.ts` knowing what it means.
- [x] Extend `packages/cli/src/cli.test.ts` with the success-path dispatch cases: `pds sync`, `pds secret status`, `pds secret delete --yes`, and `pds login --identifier alice.example`, each asserting the function reached and the options it received through the multi-word action, with no positional shifting.
- [x] Add the environment-positional regression cases `blogwright pds sync staging` and `blogwright pds secret status staging`, each asserting the context was built for `staging` and not `production`. These are exactly what `runPds` handled at `packages/cli/src/cli.ts:196` and what task 10's dispatcher must now handle; without them the deletion in this task silently reroutes every environment-qualified pds invocation to production and CI stays green.
- [x] Add the failure cases against task 07's pinned assertions: `blogwright pds` with no action and `blogwright pds bogus` both exit non-zero with a message of the same shape as today's `unknown pds action: …`.
- [x] Record why `packages/cli/src/commands.ts:2` keeps its static `syncAfterDeploy` import - no lifecycle hooks in the SPI, pds ships by default - as a comment at the import or a line in `DEVELOPMENT.md`, and add the test asserting `deploy` reaches `syncAfterDeploy` for `production` with a `pds` block and skips it otherwise.
- [x] Run `pnpm knip` after the namespace import is gone and clear anything it orphans in `packages/cli`.

## Definition of done

- [x] `packages/cli/src/cli.ts` contains no reference to pds - no `runPds`, no `PdsValues`, no `blogwright-pds` import, no `command === 'pds'` branch, no static pds lines in `USAGE` - verifiable by grepping the file for `pds` and getting zero hits.
- [x] The environment positional survives the migration: `blogwright pds sync staging` and `blogwright pds secret status staging` build a context for `staging`, asserted by test. `runPds` resolved this at `cli.ts:196`; if generic dispatch does not, every environment-qualified pds command silently targets production.
- [x] Dispatch tests cover the success paths: `blogwright pds sync` reaches `sync`; `blogwright pds secret status` and `blogwright pds secret delete --yes` reach `secretStatus`/`secretDelete` with the right options via the declared multi-word action and no positional shifting; `blogwright pds login --identifier alice.example` passes the identifier through; and `blogwright pds` with no action and `blogwright pds bogus` both exit non-zero with a message of the same shape as today, asserted against the pinned assertions from task 07.
- [x] `blogwright --help` still lists all six pds actions, each with a one-line summary, produced at runtime from the plugin's `description` and its commands' `summary` fields; any guidance lost from the current multi-line `pds login` and `pds sync` help text is deliberate and named in the commit description - it is a small but real user-visible regression.
- [x] `packages/cli/src/commands.ts:2` still statically imports `syncAfterDeploy` from `blogwright-pds` and `blogwright-pds` remains a non-optional `dependencies` entry of `packages/cli/package.json:28`, with the reason (no lifecycle hooks in the SPI; pds ships by default) recorded at the import or in `DEVELOPMENT.md`; a test asserts `commands.deploy` reaches `syncAfterDeploy` for `env === 'production'` with a `pds` block and skips it otherwise, so the post-deploy sync path is provably intact.
- [x] Meets the repo definition of done (see plan.md baseline).
- [x] Reviewable: run `pnpm --filter blogwright exec vitest run cli --reporter=verbose` and `pnpm knip` from the repo root, then `grep -n "pds" packages/cli/src/cli.ts`; confirm zero hits, the six dispatch cases pass, and `blogwright --help` output still names all six actions.
