# Task 59 — Remove the pds branch from the site's OIDC policy

**Plan:** [plan.md](../plan.md) · **Certificate:** [59-cli_drop_pds_from_site_graph-certificate.md](59-cli_drop_pds_from_site_graph-certificate.md)

**Implements:** [2026-07-26-migrate_pds_to_plugin_system.md §`blogwright-cli` → The site graph drops its pds branch (Remove)](../../../changes/2026-07-26-migrate_pds_to_plugin_system.md) and [2026-07-26-cli_plugin_system.md §Plugin SPI → A plugin owns its own topography (Add)](../../../changes/2026-07-26-cli_plugin_system.md)
**Depends on:** 23, 29
**Produces:** the CLI's resource graph carries no pds knowledge — no `config.pds` branch, no secret-name interpolation, no inline `<siteName>/atproto` default — with the grant owned entirely by the plugin's own node, and the pds change spec merged now that its last `Proposed changes` block has landed

**Pointers:** `packages/cli/src/nodes.ts:913-927` (the `if (ctx.config.pds)` block to delete, with the secret ARN at `:925` and task 27's inline `<siteName>/atproto` default inside it), `packages/cli/src/nodes.ts:863` (`oidcRolePolicyStatements`, the exported function under test), `packages/cli/src/nodes.ts:823` (the doc comment mentioning "read access to the PDS credentials secret", which must lose that clause), `packages/cli/src/nodes.test.ts:194-208` (the ARN assertion that moves to the pds package at task 23) and task 27's `undefined`-guard case beside it, `packages/pds/src/nodes.ts` (task 23 — where the grant now lives), `packages/cli/src/nodes.ts:840-842,962` (`applyOidcRole` rewriting the whole `<env>-deploy` document on every `blogwright bootstrap` — why this removal is felt on the next bootstrap of a deployed stack, not at upgrade time), [the pds change spec](../../../changes/2026-07-26-migrate_pds_to_plugin_system.md) (its `Status:` line, §Upgrading a deployed stack, and its §Merge plan steps 4–5, which task 30 deferred here), `.specs/README.md` (the pending list, whose last entry this task removes)

## Steps

- [ ] Delete the `if (ctx.config.pds)` statement block at `packages/cli/src/nodes.ts:913-927` — including task 27's inline `<siteName>/atproto` default, which exists only to keep that block's ARN total and dies with it — and correct the doc comment at `:823` so it no longer claims the role grants access to the PDS secret.
- [ ] Run `pnpm knip` and clear anything the removal orphans. `nodes.ts` imports nothing from `blogwright-pds` before this task and must import nothing after: the grant moved by way of the plugin contributing its own node (task 23), not by the CLI reaching into the plugin, so there is no import to delete here — confirm that with `grep -c blogwright-pds packages/cli/src/nodes.ts` returning `0`.
- [ ] Move the ARN assertions out of `packages/cli/src/nodes.test.ts:194-208` — task 23 already reproduces the policy document against the plugin's node, and task 27's `undefined` guard has nothing left to guard — so delete both here rather than duplicating coverage, and add their negative: a context WITH `config.pds` set produces an OIDC policy containing no `secretsmanager` statement at all.
- [ ] Write the changeset and the upgrade note, and state the mechanism correctly. The old grant is a **statement inside** the `<env>-deploy` inline policy, not a separate policy: `applyOidcRole` replaces that whole document on every `blogwright bootstrap` (`packages/cli/src/nodes.ts:840-842,962`), so it does not survive until the operator acts — it survives until their next site bootstrap. The note therefore reads: run `blogwright pds bootstrap` once before your next `blogwright bootstrap`; a stack that skips it loses the grant at that point, the post-deploy sync warns without failing the deploy, and `blogwright pds bootstrap` heals it.
- [ ] Execute the pds spec's §Merge plan steps 4–5, which task 30 deferred here because this task lands its §The site graph drops its pds branch block: flip that spec's `Status:` to `Merged` with a `Merged:` date, move it to `.specs/changes/merged/`, re-point every relative link inside the moved file (`../../packages/…` and `../../DEVELOPMENT.md` gain a level; its sibling links to the other two specs become `merged/`-relative, since task 58 has already moved them), and remove its entry from `.specs/README.md`'s pending list, which this leaves empty.

## Definition of done

- [ ] `oidcRolePolicyStatements` contains no reference to `config.pds`, no `atproto` default and no `secretsmanager` statement for any input, asserted by a test using a context that HAS a `pds` block — the positive-looking case that would otherwise hide the residue.
- [ ] `grep -n "pds\|atproto" packages/cli/src/nodes.ts` returns zero hits, closing the gap task 29's `cli.ts`-only gate left open and retiring task 27's deliberate duplication in the same move.
- [ ] The grant is provably preserved end to end: with the pds plugin bootstrapped, the site role carries a `blogwright-pds` inline policy whose document matches what the site used to emit (task 23's assertion), so a CI deploy's access is unchanged.
- [ ] `pnpm knip` passes; a changeset records the upgrade note, and that note describes the real mechanism — the grant is a statement inside `<env>-deploy`, replaced wholesale by the next `blogwright bootstrap`, so the instruction is "run `blogwright pds bootstrap` before your next site bootstrap", not "it survives until you run it".
- [ ] This ships in a **later release** than task 30, not the same one, and the change description says so: the plugin's policy only reaches a real role when an operator runs `blogwright pds bootstrap`, which needs a release's worth of notice. A reviewer who finds task 30 unreleased treats this task as blocked.
- [ ] The pds change spec's `Status:` is `Merged` with a `Merged:` date, the file sits in `.specs/changes/merged/` with every relative link re-pointed, and `.specs/README.md`'s pending list is empty — the flip task 30 deferred here, because a spec is not merged while one of its `Proposed changes` blocks is outstanding.
- [ ] Meets the repo definition of done (see plan.md baseline).
- [ ] Reviewable: run `pnpm test -- nodes` and `pnpm knip`; confirm the with-pds context yields no secretsmanager statement, that `grep -nE "pds|atproto" packages/cli/src/nodes.ts` is empty, and that every link in `.specs/README.md` resolves.
