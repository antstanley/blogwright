# Task 52 - Guard the shared CloudFront delivery source against cascading deletes

**Plan:** [plan.md](../plan.md) · **Certificate:** [52-cli_shared_delivery_source_guards-certificate.md](52-cli_shared_delivery_source_guards-certificate.md)

**Implements:** [2026-07-26-analytics_plugin.md §CloudFront log delivery → Two guards on the site's node (Modify)](../../../changes/2026-07-26-analytics_plugin.md) and [2026-07-26-cli_plugin_system.md §CLI → Plugin lifecycle (Add)](../../../changes/2026-07-26-cli_plugin_system.md) (a site node deletes only what it created)
**Depends on:** 37
**Produces:** `deliveriesForSource` returns each delivery's destination ARN alongside its id, and on that `logDeliveryNode` refuses to remove a delivery source that carries deliveries it does not own - in `delete()` AND in its `ConflictException` retry, which additionally deletes only the site's own delivery - so the analytics delivery survives both `blogwright destroy` and a bootstrap self-heal

**Pointers:** `packages/cli/src/nodes.ts:713-777` (`logDeliveryNode` in full), `:743-762` (the `ConflictException` retry that today deletes every delivery via `deliveriesForSource`, and its only caller), `:763-775` (`delete()` and the teardown-ordering comment), `packages/core/src/aws/logs.ts:124-136` (`findDeliveryIdBySource` - `.find()`, returns the FIRST match; unchanged by this task and unusable for the guards), `packages/core/src/aws/logs.ts:139-153` (`deliveriesForSource`, whose `Promise<string[]>` return this task widens - `:143-146` is the `DescribeDeliveries` response type, `:148` is where only `d.id` is kept today), `packages/core/src/aws/logs.ts:164-171` (`deleteDeliverySource`, which catches only `isNotFound` so a Conflict propagates), `packages/cli/src/nodes.test.ts:32-105` (the recording `logsUsEast1` fake and its call-order assertions), `packages/cli/src/nodes.test.ts:63` (the fake's `deliveriesForSource` returning `['d-1']`, which this task updates to the widened shape)

> **CORRECTED 2026-08-30, from task 16's verification gate.** The four remedy
> strings in this task now carry `<env>`. An env-less `blogwright analytics
> destroy` resolves the environment as `values.env ?? envPositional ??
> DEFAULT_ENV` with `DEFAULT_ENV = 'production'` (`plugin-commands.ts:138,675`),
> so a refusal raised while tearing down staging or a preview hands the operator
> a command that targets PRODUCTION. Best case it loads empty state and removes
> nothing, leaving them stuck in the same refusal; worst case it tears down the
> live production analytics stack. `deriveNames` keys off `env` alone, so the
> environment is the whole difference. Interpolate the env this node is running
> against - the message must be runnable exactly as printed, and a test that
> asserts only the scope name would pass on the broken form.

## Steps

- [ ] Give the guards something to discriminate on before writing them: widen `deliveriesForSource` (`packages/core/src/aws/logs.ts:139-153`) from `Promise<string[]>` to a list of `{ id, deliveryDestinationArn }`: add the field to the response type at `:144` - `DescribeDeliveries` already returns it beside `id` - and carry it through the filter at `:147-149`, which today keeps `d.id` alone. Neither existing lookup can express "the site's own delivery" - `findDeliveryIdBySource` returns a single id chosen by `.find()` and `deliveriesForSource` drops everything but `d.id` - and DEVELOPMENT.md §Where validation lives forbids the workaround, because the CLI never issues a raw AWS call. The widening lands here rather than in task 37 because the retry loop at `packages/cli/src/nodes.ts:753-757` is its only caller and this task rewrites it; update the two-line fake at `packages/cli/src/nodes.test.ts:63` to the new shape in the same change. Leave `findDeliveryIdBySource` untouched - `delete()` stops using it.
- [ ] In `delete()`, read `deliveriesForSource` first and refuse BEFORE deleting anything when the source carries a delivery the site does not own: leave the whole trio in place and fail with a message naming the foreign delivery and pointing at `blogwright analytics destroy <env>`. Refusing first rather than after removing the site's own delivery is the difference between an actionable stop and a half-torn-down stack. When the site's is the only delivery, keep today's order exactly - delivery, then source, then destination (`packages/cli/src/nodes.ts:763-775`) - so the sequence the teardown comment documents is unchanged. Today the source is deleted unconditionally, and with a second delivery attached AWS returns a Conflict that `deleteDeliverySource` does not catch - so `blogwright destroy` throws partway through teardown, after the distribution is already gone.
- [ ] In the `ConflictException` retry, delete only the site's own delivery id rather than iterating `deliveriesForSource` and deleting all of them. The current loop silently removes the analytics delivery while the plugin's scoped state still records it as `configured`, so `analytics status` reports healthy and CloudFront has stopped delivering - a silent data-loss path, which is the worst shape a bug can take here.
- [ ] Apply the SAME refusal to the retry, and put it before any delete. Scoping the delivery deletion alone leaves `deleteDeliverySource` (`packages/cli/src/nodes.ts:758`) unconditional - the exact call `delete()`'s guard exists to prevent, on the one path that guard does not cover. With the plugin's delivery attached AWS rejects it and `deleteDeliverySource` catches only `isNotFound` (`packages/core/src/aws/logs.ts:164-171`), so `blogwright bootstrap` throws *after* deleting the site's own delivery: a stack left with no CloudWatch delivery and a failed bootstrap, strictly worse than the conflict it was healing. The retry cannot succeed there by any other route either - `putDeliverySource` refuses to repoint an existing source, so removing the source IS the retry - which is why this is a refusal and not a workaround. Call the same named predicate and raise the same message naming `blogwright analytics destroy <env>`.
- [ ] Make the fake capable of failing before trusting either guard: the recording `deleteDeliverySource` at `packages/cli/src/nodes.test.ts:67-69` returns void unconditionally, so a retry that still deletes the shared source passes every assertion about which delivery ids were deleted. Give the fake AWS's behaviour - reject `deleteDeliverySource` while any delivery remains on the source - so the guards are exercised in the only direction that matters. A fake that cannot fail is why this hole survived four spec revisions.
- [ ] Identify "the site's own delivery" as the entry whose `deliveryDestinationArn` names `ctx.names.deliveryDestination`, not by position and not from state. Match on the name the ARN carries in its final segment, in one named predicate both guards call: the recorded `destination` output (`packages/cli/src/nodes.ts:731`) is empty exactly when the retry needs it, because `putDeliverySource` throws the Conflict at `:721-726` before `putDeliveryDestination` ever runs. The name is deterministic for the environment, so it also matches the previous stack's delivery on a destroy → bootstrap cycle - which is the delivery the retry is there to remove. `findDeliveryIdBySource` uses `.find()` (`logs.ts:131`) and returns whichever delivery AWS lists first, which is not necessarily the site's once a second exists; the widened list, not that lookup, is what both guards read.
- [ ] Extend `packages/cli/src/nodes.test.ts` with the recording `logsUsEast1` fake, whose `deliveriesForSource` at `:63` now returns `{ id, deliveryDestinationArn }` entries: a `delete()` against a source carrying one foreign delivery leaves the source alive and raises with that delivery named; a `delete()` against a source carrying only the site's own removes all three in the documented order; a Conflict retry with a foreign delivery present deletes only the site's id and leaves the foreign one in the call log. Add the `LogsClient` half in `packages/core/src/aws/logs.test.ts`: a paginated `DescribeDeliveries` response maps to ids paired with their destination ARNs, and a delivery on another source is still filtered out.
- [ ] Write the changeset: `blogwright destroy` can now fail where it previously threw a Conflict, which is the point - it fails early with an actionable message instead of part-way through.

## Definition of done

- [ ] `deliveriesForSource` returns each delivery's id together with its `deliveryDestinationArn`, so "the site's own delivery" is expressible without a raw AWS call from the CLI; a `LogsClient` test asserts the mapped shape from a paginated `DescribeDeliveries` response, and `findDeliveryIdBySource` is unchanged.
- [ ] `delete()` never deletes a delivery source carrying a delivery it does not own; it fails with a message naming the foreign delivery and `blogwright analytics destroy <env>`, asserted by test, and the source is still present in the fake's state afterwards.
- [ ] With no foreign delivery present, `delete()` still removes delivery → source → destination in the order the comment at `nodes.ts:763-768` documents, proved by a NEW call-order assertion on the teardown path - there is none today, and the two existing sequences at `packages/cli/src/nodes.test.ts:88-97,104` are `create()`/retry paths that prove nothing about `delete()`. This task must not regress the existing teardown.
- [ ] The `ConflictException` retry deletes exactly one delivery id, the site's own, identified by the one named predicate matching `deliveryDestinationArn` against `ctx.names.deliveryDestination` rather than by list position or by a state output the retry path has not written yet; a test with a foreign delivery present asserts the foreign id never appears in a delete call.
- [ ] The retry never removes the shared delivery source either: with a foreign delivery present it refuses before issuing any delete, raising the same message naming `blogwright analytics destroy <env>` that `delete()` raises, and the recording fake's delete list is empty. The fake used for that test REJECTS `deleteDeliverySource` while deliveries remain - a fake that returns void makes the guard's absence invisible, which is exactly how the unconditional `deleteDeliverySource` at `nodes.ts:758` survived the earlier reviews - and both guards call one predicate, greppable to a single definition.
- [ ] The existing self-heal behaviour is preserved for the single-delivery case: the two call-order assertions at `packages/cli/src/nodes.test.ts:88-97,104` pass with no edit to any expected sequence - the only permitted change in that block is the `deliveriesForSource` fake at `:63` returning the widened shape.
- [ ] Meets the repo definition of done (see plan.md baseline).
- [ ] Reviewable: run `pnpm --filter blogwright exec vitest run nodes --reporter=verbose` and `pnpm --filter blogwright-core exec vitest run logs --reporter=verbose` in `packages/core`; confirm the foreign-delivery cases fail loudly rather than cascading, that the widened `deliveriesForSource` still filters by source name, and that no assertion in the pre-existing log-delivery tests had to be edited.
