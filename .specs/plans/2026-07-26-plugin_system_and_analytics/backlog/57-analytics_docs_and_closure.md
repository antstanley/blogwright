# Task 57 — Document the analytics plugin, update the toolchain and ports tables, and close the change spec

**Plan:** [plan.md](../plan.md) · **Certificate:** [57-analytics_docs_and_closure-certificate.md](57-analytics_docs_and_closure-certificate.md)

**Implements:** [2026-07-26-analytics_plugin.md §Merge plan](../../../changes/2026-07-26-analytics_plugin.md) (all five steps, of which steps 3–5 are actionable today: the toolchain and ports rows, the `Status:` flip and move, and the `.specs/README.md` update) and §Ports → `AnalyticsQuery` (the row the ports table gains) and §Analytics plugin → Namespace and commands (the five actions the package README documents)
**Depends on:** 54, 56
**Produces:** the Vite/SvelteKit toolchain row and the `AnalyticsQuery` ports row in DEVELOPMENT.md, a `packages/analytics/README.md` documenting install, the five actions, the us-east-1 pinning and the privacy contract, a changeset stating the semver impact, the spec's five open questions each resolved or owned, and the change spec merged so no pending change specs remain
**Pointers:** `DEVELOPMENT.md:12-22` (the Toolchain table the Vite/SvelteKit row joins, below the `rolldown` row at `:21`), `DEVELOPMENT.md:72-81` (§Hexagonal architecture's ports table the `AnalyticsQuery` row joins, in the same four-column shape as the `Vcs` row at `:80`), `DEVELOPMENT.md:246-251` (§Documentation: doc comments on public exports, a module comment stating what each module owns, no bare `// TODO` without an owner), `packages/analytics/README.md` (new — `packages/pds/README.md:1-10` is the shape: what the package owns, what it depends on, and where the command surface is documented), `packages/analytics/src/ports.ts` (task 45 — where `AnalyticsQuery` is defined and where the fixture-backed fake named in the ports row lives), `packages/analytics/src/adapters/duckdb-query.ts` (task 46 — the real adapter named in the ports row), `packages/analytics/src/config.ts` (task 44 — the recorded table-bucket-per-environment decision that answers the third open question), `packages/analytics/src/transform/visitor-key.ts` (task 41 — the recorded salt-stability decision that answers the fourth open question), `.changeset/config.json:5` (the `fixed` group `["blogwright", "blogwright-core", "blogwright-pds"]` — decide explicitly whether `blogwright-analytics` joins it), `.specs/changes/2026-07-26-analytics_plugin.md:3` (the `Status:` header line to flip and where the `Merged:` date goes), `.specs/changes/2026-07-26-analytics_plugin.md:342-352` (the five-step merge plan), `.specs/changes/2026-07-26-analytics_plugin.md:411-432` (the five open questions to triage), `.specs/README.md:19-33` (the pending list, which this task empties), `.specs/README.md:35-42` (the merged list this spec joins)

## Steps

- [ ] Triage FIVE open questions, not four. Besides backfill, table record expiration, one-bucket-per-environment and salt semantics, the analytics spec also asks whether `blogwright destroy` should refuse (or warn) while `state/<env>.analytics.json` shows live resources — its delivery references the site's distribution and shared delivery source. Task 20 carries the general form forward, but it lands in M3 before any analytics resource exists, so this concrete case needs an owner here. Record each as resolved or explicitly scoped out, with an owner.
- [ ] Add the Vite/SvelteKit row to the Toolchain table at `DEVELOPMENT.md:12-22`, stating the version channel and that it builds only the analytics dashboard (`packages/analytics/app` → `dist/app`), in the same column shape as the `rolldown` row at `:21`.
- [ ] Add the `AnalyticsQuery` row to the ports table at `DEVELOPMENT.md:72-81` naming the defining module (`analytics/src/ports.ts`), the real adapter (`createDuckDbAnalyticsQuery`, `analytics/src/adapters/duckdb-query.ts`) and the fixture-backed test fake, matching the `Vcs` and `PingBuilder` rows.
- [ ] Write `packages/analytics/README.md` covering install (`blogwright plugin add analytics`), all five actions (`init`, `bootstrap`, `status`, `dashboard`, `destroy --yes`), the us-east-1 pinning and why CloudFront forces it, and the privacy contract: the raw viewer IP is never stored, and `cs(Cookie)` and `x-forwarded-for` are never selected so they never leave CloudFront.
- [ ] Sweep the package for the documentation rules at `DEVELOPMENT.md:246-251` — a doc comment on every public export of `blogwright-analytics` and an opening comment on every module stating what it owns — and remove any bare `// TODO` left by tasks 38–56 or give it an owner and a reference.
- [ ] Triage the spec's five open questions at `.specs/changes/2026-07-26-analytics_plugin.md:411-432` in the change description: backfill and table record expiration are recorded as out of scope with an owner, one-bucket-per-environment is recorded as settled at task 44 and salt stability as settled at task 41, each pointing at where the decision now lives in code.
- [ ] Write the changeset: a new package `blogwright-analytics` plus a minor on `blogwright-core` for the four service clients and the `LogsClient` delivery parameters, and record whether `blogwright-analytics` joins the fixed group at `.changeset/config.json:5` or versions independently.
- [ ] Execute the merge plan: flip `Status:` to `Merged` with a `Merged:` date at `.specs/changes/2026-07-26-analytics_plugin.md:3`, move the file to `.specs/changes/merged/`, and rewrite `.specs/README.md:19-42` so the pending section records that no proposals are pending and the merged list gains all three specs of this plan.

## Definition of done

- [ ] `DEVELOPMENT.md`'s Toolchain table gains the Vite/SvelteKit row naming the dashboard build as its only use, and §Hexagonal architecture's ports table gains `AnalyticsQuery` with its defining module, its real adapter (`packages/analytics/src/adapters/duckdb-query.ts`) and its fixture-backed test fake, in the same column shape as the existing rows.
- [ ] `packages/analytics/README.md` documents install (`blogwright plugin add analytics`), all five actions, the us-east-1 pinning, and states that the raw viewer IP is never stored and that `cs(Cookie)` and `x-forwarded-for` are never selected — and the same documentation obligation holds inside the code: every public export of `blogwright-analytics` carries a doc comment and every module opens with a comment stating what it owns.
- [ ] Each of the spec's five open questions — backfill, table record expiration, one bucket per environment, salt stability — is either resolved in the change description (the last two are settled at tasks 44 and 41 and are recorded here with the module that holds the decision) or recorded as out of scope with an owner, and no bare `// TODO` exists in the new code — `grep -rn "TODO" packages/analytics/` returns nothing without an owner and a reference.
- [ ] A changeset describes the semver impact — a new package plus a minor on `blogwright-core` for the four clients and the `LogsClient` delivery parameters — and the change spec's merge plan is executed: `Status:` flipped to `Merged` with a `Merged:` date, the file moved to `.specs/changes/merged/`, and `.specs/README.md` updated so no pending change specs remain and every link resolves.
- [ ] Meets the repo definition of done (see plan.md baseline).
- [ ] Reviewable: run `pnpm build && pnpm test && pnpm lint && pnpm exec oxfmt --check . && pnpm knip`, then confirm `.specs/changes/` holds only `merged/`, that `.specs/changes/merged/2026-07-26-analytics_plugin.md` carries a `Merged:` date, and that every link in `.specs/README.md` resolves to an existing file.
