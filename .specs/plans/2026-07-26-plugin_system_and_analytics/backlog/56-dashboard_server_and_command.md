# Task 57 — The loopback dashboard server and the analytics dashboard command

**Plan:** [plan.md](../plan.md) · **Certificate:** [56-dashboard_server_and_command-certificate.md](56-dashboard_server_and_command-certificate.md)

**Implements:** [2026-07-26-analytics_plugin.md §Analytics dashboard → Local server](../../../changes/2026-07-26-analytics_plugin.md) (an HTTP server bound to `127.0.0.1` serving the prebuilt application and answering a fixed set of named, parameterised queries — never SQL supplied by the client — each taking a date range and a bot-inclusion flag) and §Analytics dashboard → Credentials (the plugin resolves credentials through blogwright's existing provider chain and passes them into the adapter) and §Analytics plugin → Namespace and commands (`analytics dashboard` — starts the local dashboard server)
**Depends on:** 46, 47
**Produces:** `packages/analytics/src/server.ts` — the package's only `node:http` importer, bound to `127.0.0.1` on the configured port, routing to named queries alone with no route that accepts SQL — plus the `analytics dashboard` command that constructs the DuckDB adapter at the plugin's composition root, prints the URL, and releases the listener on shutdown
**Pointers:** `packages/analytics/src/server.ts` (new — the package's edge module; the only place `node:http` may be imported), `packages/analytics/src/server.test.ts` (new — the loopback-bind, unknown-name, SQL-rejection, parameter-passthrough and shutdown tests), `packages/analytics/src/commands.ts` (task 47 — the command implementations the plugin's table points at; the `dashboard` handler is the plugin's composition root), `packages/analytics/src/plugin.ts` (task 47 — the `commands` table the `dashboard` action and its `summary` join), `packages/analytics/src/queries.ts` (task 45 — the named set and the unknown-name lookup the routes resolve through), `packages/analytics/src/ports.ts` (task 45 — `AnalyticsQuery.run(name, params)`, the only surface the routes touch, plus the fixture-backed fake the tests substitute), `packages/analytics/src/adapters/duckdb-query.ts` (task 46 — `createDuckDbAnalyticsQuery`, constructed only here), `packages/analytics/src/config.ts` (task 44 — the resolved `dashboard.port` and its exported default constant, and the `bots` default behind the bot-inclusion flag), `packages/core/src/aws/credentials.ts:19,44` (`createCredentialProvider` for the real chain, `staticCredentials` for the tests), `packages/build-agent/src/server.ts:106-118` (the repo's only `createServer`/`listen` precedent — it binds `0.0.0.0` and reads `PORT` from the environment, and the dashboard must do neither), `packages/cli/src/context.ts:110-116` (the composition-root precedent: adapters constructed in exactly one place), `packages/cli/src/logger.ts:9-15` (the `Logger` surface the command prints the URL through)

## Steps

- [ ] Write `packages/analytics/src/server.ts` exporting `createDashboardServer({ query, port, appDir })` returning a handle carrying the resolved address and a `close()`, with a module doc comment stating that this module is the package's edge — the one place `node:http` is imported and the one place a request becomes a port call.
- [ ] Bind the listener explicitly to the loopback address as a named module constant (`const LOOPBACK_ADDRESS = '127.0.0.1'`) and take the port from the resolved `config.analytics.dashboard.port` the caller passes in, never from a literal at the call site and never from `process.env`.
- [ ] Route the request surface as exactly two families — static assets served from `appDir` (task 57's `dist/app`) and one named-query route per entry in `queries.ts` — resolving the requested name through task 45's lookup so an unknown name answers 404 with the available names, and add no route, method, or body handler that accepts SQL text.
- [ ] Parse the date range and the bot-inclusion flag from the request at the boundary, validate them there through task 45's parameter validation, and hand them to `AnalyticsQuery.run(name, params)` unmodified, so no request value is ever concatenated into a query.
- [ ] Implement the `dashboard` handler in `packages/analytics/src/commands.ts` as the plugin's composition root: resolve credentials through `createCredentialProvider` (`packages/core/src/aws/credentials.ts:19`), construct `createDuckDbAnalyticsQuery` there and nowhere else, start the server, print the resolved URL through `ctx.logger.info`, and register the shutdown path that awaits `close()` on SIGINT and on an explicit stop.
- [ ] Fill in the `dashboard` handler in `packages/analytics/src/commands.ts` — do NOT add an entry to the command table. Task 47 writes that table once, declaring `dashboard` with its `summary` and pointing it at a named function that raises until this task fills it in, and task 47's DoD says no later task edits the table.
- [ ] Write `packages/analytics/src/server.test.ts` driving the server through the fixture-backed fake `AnalyticsQuery`: the bind address and port assertions, the unknown-name 404 listing available names, a request carrying SQL in its body and in its query string asserted rejected with the fake recording zero calls, a parameter-passthrough assertion comparing the received `params` object to the request values, and a shutdown assertion that a second listener binds the same port after `close()` resolves.

## Definition of done

- [ ] `analytics dashboard` starts an HTTP server bound to `127.0.0.1` on `config.analytics.dashboard.port`, prints the resolved URL, and shuts down cleanly — tests assert the bind address is the loopback constant rather than `0.0.0.0` or a wildcard, that the port arrives from the resolved config (task 44's exported default) and not a literal, and that after the shutdown path resolves a second listener binds the same port.
- [ ] The server routes only to named queries: a request naming an unknown query returns 404 listing the available names, and no route accepts SQL — a negative-space test posts SQL as a body and as a query parameter and asserts both are rejected before the port is reached, with the fixture-backed fake `AnalyticsQuery` recording zero `run` calls.
- [ ] Each query route takes a date range and a bot-inclusion flag and passes them as `params` to `AnalyticsQuery.run(name, params)`; a test with the fixture fake asserts the parameters arrive unmodified, and an invalid or inverted range is rejected at the request boundary rather than defaulted.
- [ ] The listener is the package's edge module and the DuckDB adapter is constructed only at the plugin's composition root — `grep -rn "node:http\|node:fs\|@duckdb" packages/analytics/src/` matches `server.ts` for `node:http` and `adapters/duckdb-query.ts` for `@duckdb` only, so `queries.ts` and every data-shaping module import neither.
- [ ] Meets the repo definition of done (see plan.md baseline).
- [ ] Reviewable: run `pnpm test -- server` inside `packages/analytics`, then `grep -rn "0.0.0.0\|node:http" packages/analytics/src/`; confirm the only `node:http` match is `server.ts`, that no `0.0.0.0` appears anywhere, and that deleting the unknown-name guard makes the 404 test fail naming the requested query.
