# Task 00 - Own the type-claim gate that compiles the corpus's compiler claims

**Plan:** [plan.md](../plan.md) · **Certificate:** [00-type_claim_gate-certificate.md](00-type_claim_gate-certificate.md)

**Implements:** the mechanical check for every type-level claim the three change specs make - [2026-07-26-cli_plugin_system.md](../../../changes/2026-07-26-cli_plugin_system.md) (§The `Plugin` contract's `never` default and registry bivariance, §`PluginContext`'s sixteen-member enumeration, §Typed plugin config's `TS2339`/`TS7053`, §The two state surfaces' `TS2542`/`TS18048`, §Vocabulary relocation's unconstrained `ResourceNode<Ctx>` and structural engine constraint, §Plugin-supplied AWS services' closed `ServiceKey` premise, the Assumptions' `TS2739`), [2026-07-26-migrate_pds_to_plugin_system.md](../../../changes/2026-07-26-migrate_pds_to_plugin_system.md) (§Context's `Pick` narrowing and nine left-out members, §Config ownership's `TS2345` and `secretName` widening, §Type changes' `ResolvedPdsConfig`) and [2026-07-26-analytics_plugin.md](../../../changes/2026-07-26-analytics_plugin.md) (§Configuration's resolved `AnalyticsConfig` joining the plugin registry). No production code implements this task; it is plan infrastructure
**Depends on:** -
**Produces:** [`type-claims/`](../type-claims/README.md) - a transcription of the proposed SPI types with per-declaration spec citations (`transcriptions.ts`), 29 compiled assertions in both directions with per-claim citations and the exact diagnostic codes the documents quote (`claims.ts`), a tsconfig extending the root `tsconfig.base.json` that imports the REAL `OpsContext`, `OpsConfig`, `OpsState`, `AwsClients`, `PdsContext`, `StateStore`, `ServiceKey` and `SendOptions` from `packages/`, and a runner (`check.mjs`) that exits non-zero naming each broken claim
**Pointers:** [`../type-claims/README.md`](../type-claims/README.md) (what the gate is for, how to run it, and the update rule), `../type-claims/transcriptions.ts` (the proposed types - the file that moves when a spec moves), `../type-claims/claims.ts` (the assertions; negative claims under `@ts-expect-error` so a claim that silently stops erroring fails as `TS2578`), `../type-claims/check.mjs` (the runner - repo TypeScript resolved from `packages/cli`, no network, no AWS), `tsconfig.base.json` (the strictness the quoted diagnostics depend on: `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`), `packages/cli/src/context.ts:25-51` (`OpsContext` - thirteen of `PluginContext`'s sixteen members), `packages/pds/src/context.ts:25` (the real `PdsContext` claim C20 pins)

## Steps

- [x] Build the harness: transcriptions with citations, claims in both directions, the runner, the README with the update rule. Verified 2026-07-27: `node check.mjs` reports 29 claims held (12 compiled positives, 17 pinned compile-errors); with the superseded `Ctx extends PluginContext` constraint reintroduced into the transcription, the gate fails naming claim C16 (task 02 / §Vocabulary relocation) with the same `TS2344` the seventh review found by hand; `pnpm knip` and `pnpm build` are unaffected because the directory sits outside `packages/`.
- [ ] Run `node .specs/plans/2026-07-26-plugin_system_and_analytics/type-claims/check.mjs` whenever a spec's proposed type changes or a review round amends a task that quotes a diagnostic, updating `transcriptions.ts` to the spec first - the failure list is the set of documents now asserting a stale truth.
- [ ] Retire the three ground-truth-today claims as their tasks land, per the README: C23 at task 27 (`PdsConfig.secretName` widens), C26 at task 31 (the transport seam opens), C28 at task 38 (`AwsClients` gains `signingUsEast1`). Each retirement is a deliberate deletion with the landing task named in the commit description, never a silent edit.
- [ ] As SPI types land in `packages/core` for real (tasks 01–04), replace the corresponding transcriptions with imports of the real declarations, so the gate stops checking a copy the moment ground truth exists - the transcription of a type that now exists is itself the stale-truth defect this gate hunts.
- [ ] At plan close (task 58/59), decide whether the gate graduates into CI or retires with the plan, and record the decision in the plan's open questions. It is deliberately NOT wired into CI or the root `package.json` scripts today: the plan proposes that wiring and does not do it, because the gate checks planning artifacts, not shipped code, and the repo's CI gates are reserved for the five production commands (DEVELOPMENT.md §Toolchain).

## Definition of done

- [ ] `node check.mjs` exits 0 on the corpus as committed, reporting every claim held, and exits non-zero naming the broken claim (its `Cnn` id and the document section it cites) when any transcription, claim, or real type under `packages/` drifts - demonstrated by the recorded constraint-reintroduction run.
- [ ] Every transcribed declaration cites the spec section it comes from, and every claim carries the document section or task number it pins plus the exact diagnostic code that document quotes; negative claims use `@ts-expect-error` so a silently-compiling claim fails the gate as `TS2578`.
- [ ] The gate touches nothing the production toolchain owns: `pnpm knip`, `pnpm build`, `pnpm test`, `pnpm lint` and `pnpm exec oxfmt --check .` behave identically with and without the `type-claims/` directory, and no root `package.json` script or CI step names it.
- [ ] The three retirement claims (C23/C26/C28) are either still pinning ground truth or have been deliberately deleted in the commit that landed their task, with the task named - no claim is ever silenced by weakening its assertion.
- [ ] Reviewable: run `node .specs/plans/2026-07-26-plugin_system_and_analytics/type-claims/check.mjs` and observe PASS with the claim count; then temporarily add `extends PluginContext` to `ResourceNode` in `transcriptions.ts`, observe FAIL naming claim C16 with `TS2344`, and revert.
