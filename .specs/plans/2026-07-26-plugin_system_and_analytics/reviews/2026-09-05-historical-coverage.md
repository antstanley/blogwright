# Plan structure and historical coverage reconciliation

Reviewed 2026-09-05. This is a read-only reconciliation of historical evidence, not a new discharge. No tests or mutation experiments were run by this reviewer. Historical certificate statements below describe what their original validators recorded; source references identify current tests inspected, not fresh passing results. Keep historical certificates intact. Task 63 owns current closure and any evidence gaps below; 59/60 own their narrowed implementation gates; 62 owns the analytics midnight-bound correction.

## Structural baseline before remediation

62 tasks (60 done, 2 blocked), 62 colocated certificates, 62 table rows, 115 table edges matching 115 Mermaid edges exactly. Every task Depends-on field matched the table and every dependency was lower numbered, proving this recorded order is topological. Zero broken Markdown file links in the plan tree. This does not prove runtime dependency completeness: publication of task 30 must gate task 59 explicitly.

## Historical DoD items without their own certificate obligation

Numbers below are the one-based checklist items under each task's Definition of done, excluding prose amendments. Cardinality mismatch alone does not imply missing implementation or missing historical evidence.

| Task | DoD item | Existing evidence mapping | Current disposition / required evidence |
|---|---|---|---|
| 01 | D2: typed `pluginConfig.foo`, while site `config` has no such key | Certificate O1 checks interface types and O4/O6 record all context tests compiling, but neither explicitly discharges this independent generic-config claim. Current `packages/cli/src/context.test.ts:239` has `exposes pluginConfig as the plugin's own typed config block`, a no-cast `PluginContext<{foo:string}>` and runtime `foo` assertion. Its comment asserts TS2339 for `config.foo`, but does not actually compile that negative expression. | Positive test exists; explicit recorded negative-type evidence is absent in this certificate. Task 63 should identify a surviving shipped negative type check or run a temporary compile probe against real types before retiring transcriptions. Do not infer negative evidence from the comment. |
| 01 | D3: separate state surfaces, record isolation and real destroyGraph deletion | Certificate O3 explicitly records both tests, their named assertions and mutations failing them; also records a wrong-state-type compiler probe. | Historical evidence exists; map D3 to O3 (which also covers D5) without inventing an extra pass. |
| 05 | D2: real `packageJsonPathFor` integration, no `<name>/package.json`, exports error contrast | Certificate Premises gives the real resolution transcript; O3 records integration cases and the `ERR_PACKAGE_PATH_NOT_EXPORTED` contrast. Conclusion explicitly re-proves the real PDS package path. | Historical evidence exists, grouped in O3. |
| 08 | D5: one exported cliPackageDir helper, callable without context, no import.meta in plugins.ts | Certificate O2 “Checks run” records helper export and context test, candidate resolution from CLI_DIR, real-workspace lookup. O4 records imports. O2 regression check demonstrates unchanged agentDir resolution in source and built layouts. | Most evidence exists. For literal no-import.meta/single-source clause, task 63 may use current symbol/source inspection; do not claim the original certificate records the exact absence grep if it does not. |
| 08 | D7: walk past nameless nested package.json in dual-package layout | Certificate O6 “Additional checks” records deleting `isPackageManifest`'s name guard, killing exactly the adapter `walks past a nested package.json with no "name"` and discovery `discovers a plugin published with the dual-package layout` tests. | Fully mapped to O6 historical mutation evidence. |
| 08 | D8: real adapter discovery and direct package.json-resolution mutation | Certificate O6 records direct `<name>/package.json` mutation failing only the two real integration cases, with exports errors, while 12 fake cases remain green. | Fully mapped to O6 historical mutation evidence. |
| 10 | D4: discovery's three arguments, CLI directory from helper, bundled dispatch with only blogwright consumer dependency | No dedicated evidence in task10 certificate O1–O6 names this bundled fixture or helper call. Current `packages/cli/src/cli.test.ts:894` has exactly the bundled-only consumer test and checks its resolver's fromDir equals cliPackageDir. Current runPlugin uses `discover(repoRoot, cliPackageDir(), ports)`. | Test/source exist, historical explicit discharge missing. Task63 final CLI test run plus current source inspection can discharge; no new feature work needed. |
| 17 | D1: plugin list requires neither config nor AWS credentials and constructs no context | Certificate “Inherited findings”, lines 27–44, records throwing context factory, prior real binary on git-init directory without config/AWS variables, and explicitly re-executes that same property on the merged tree with nonexistent AWS config/credential files. | Historical evidence exists outside O list. |
| 27 | D4: narrow optional secretName before RegExp.test, no cast, rebuild downstream declarations | Certificate O4 explicitly reproduces TS2345 by reverting only narrowing, records `cfg.secretName !== undefined && …`, clean root typecheck and a separate core rebuild/type-consumer probe. | Fully mapped to O4 historical evidence. |
| 29 | D2: pds sync staging and two-word secret status staging resolve staging | Certificate O2 records 12 generic real-PDS-object dispatch cases, each asserting contexts.map(ctx=>ctx.env), and inherited mutations. Current `cli.test.ts:1291` and `:1301` contain the two literal requested invocations. | Historical grouped evidence exists; final current CLI test run should include them. |
| 31 | D7: ServiceKey and equivalent descriptor produce identical canonical request/Authorization | Certificate Regression check “Independent byte-identity proof”, line77, records 54 service/region/mode request pairs with full payload/header equality and frozen clock. O5 also records S3 equivalent-signature comparison. Current `packages/core/src/aws/signer.test.ts:104` and `:124` retain explicitly named S3/logs byte-identity cases. | Historical evidence exists outside O list; current signer tests cover examples. |
| 58 | D4: account for every analytics merge-plan step, executing3–4, recording1–2 not applicable and transferring5–6 | Certificate Residue mentions1–2 without full itemized discharge. Task58's decision notes and plan's “analytics merge-plan steps1 and2 not applicable” paragraph provide the accounting, and task61 actually merged analytics. Certificate remains PARTIAL for other closure obligations. | New task63 canonical pages/schema replace the prior no-target disposition. Task63 must map all merge-plan steps to current artifacts; do not merely inherit old no-canonical-target reasoning. |

Historical certificate/task cardinalities: task01 8/6;05 7/6;08 9/6;10 7/6;17 7/6;27 8/7;29 7/6;31 9/8;58 7/6. These are DoD-item / primary-obligation counts. Other differences (task09 O2'/O2'', task16 O5b, task52 O9) are supplemental validator findings and should remain supplemental.

## Two special historical records

Task20 certificate is a delta certificate, not an O1…On task certificate. Its Definition proves five corrections C1–C5, its “Inherited, not re-litigated” section records prior coverage and unchanged paths, and its Conclusion explicitly says COMPLETENESS DONE. Keep that judgment scoped to the historical delta. Task63's current public-doc and merge-step checks are the direct current evidence for documentation and closure; do not manufacture six historical O statuses.

Task58 certificate explicitly records O4 and O6 UNSATISFIED and VERDICT PARTIAL, because the plugin-system spec could not honestly merge while the site graph still read config.pds. Its own conclusion proposes transferring the outstanding work into an owning contract. Keep PARTIAL as the historical verdict. Record that current closure work is now assigned to63, with dependencies on59/60/62 and explicit spec/canonical/docs/link checks. A checked box in archived task58 is not proof its historical merge happened.

## Outstanding reconciliation checks for task63

1. Compile positive pluginConfig and negative config.foo claims against actual exported types, preserving evidence before the planning transcription gate retires.
2. Include current bundled-dispatch and context suites in the final CLI check; inspect the sole cliPackageDir source and dispatch discover arguments.
3. Account for every source spec merge-plan step against new canonical pages/schema and current docs, with no obsolete “analytics still pending” expectation.
4. Preserve historical PARTIAL58 and delta20 evidence while recording where their current obligations are now proved.
5. Run the six actual repo gates, including typecheck, and link their evidence rather than interpreting archived test counts as current.

## Other active-plan corrections already sent to root

Task60's existing eight-item DoD vs seven-obligation certificate is obsolete; new split should assign warning semantics to60 and docs/spec closure to63. Existing task60 release ordering sentence is inverted: a violation is60 shipping later than59. Existing task59 says only O5 needs rechecking after unpark; all affected current obligations/regressions should instead be verified after rebase. Type-claim retirement has now moved from the plan's prose-only task60 decision to explicit task63 ownership.
