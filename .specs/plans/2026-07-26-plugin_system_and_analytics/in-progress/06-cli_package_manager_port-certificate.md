# Done Certificate - Task 06: Add the PackageManager port and its process adapter

**Task:** [06-cli_package_manager_port.md](06-cli_package_manager_port.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-08-29

> This certificate is a verification protocol for Task 06. A validating agent discharges it:
> for each obligation, collect the named evidence, run the named checks, set the Status, then
> derive the Conclusion by the rubric below. Do not mark an obligation SATISFIED without its
> evidence; do not record DONE with any non-SATISFIED obligation.

## Definition

DONE(Task 06) ≡ every obligation O1…O6 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) - not by assertion.

## Premises

- **P1 - Goal.** A `PackageManager` port (`detect`, `add`, `remove`) exists with a lockfile-detecting process adapter, wired at `createContext` and defaulted to a recording fake in `createTestContext`.
- **P2 - Obligations.** The task is done iff O1…O6 all hold. One Oi per definition-of-done item, in DoD order; O6 is the `Reviewable:` item.
- **P3 - Invariants.** Must not break the existing `Ports` construction in `packages/cli/src/context.ts:111-116` and `packages/cli/src/test-support.ts:155-160`, nor the `no-restricted-imports` override list in `.oxlintrc.json:71-84`, which every package's `pnpm lint` run resolves against.

Validation ran against the isolated workspace `/Users/ant/code/blogwright-task-06`
(`jj diff --git` over parent `de581e55`, tasks 00/01/04 in the base).

## Obligations

- **O1 - Domain vocabulary and port-mediated detection.**
  - *Claim:* `PackageManager` exposes `detect(repoRoot)`, `add(spec, opts)` and `remove(name)` in the repo's own vocabulary, and detection reads lockfiles through the `FileSystem` port rather than `node:fs`.
  - *Evidence collected:* `packages/cli/src/ports.ts:17-44` declares `PackageManagerName`, `AddPackageOptions { dev?: boolean; exact?: boolean }` and the three-method `PackageManager`; the options are repo concepts, and every manager's flag spelling (`--save-dev`, `--dev`, `--save-exact`, `--exact`) is confined to the adapter's private `PACKAGE_MANAGERS` table (`packages/cli/src/adapters/process-package-manager.ts:31-64`). `grep -rn "node:fs" packages/cli/src` returns the adapter only in a prose comment (`:4`), never an import; the adapter imports `node:child_process`, `node:path`, `node:util` and `blogwright-core`.
  - *Checks:* the `fs` inside `detectManager` resolves to the `FileSystem` parameter closed over by `createProcessPackageManager(fs)` (`:105-118`), which `createContext` supplies from the same instance it puts on `ports.fs` (`packages/cli/src/context.ts:112-118`). No module-level `createNodeFileSystem()` exists in the adapter.
  - *Status:* SATISFIED

- **O2 - Lockfile mapping, positive and negative.**
  - *Claim:* each supported lockfile maps to its manager, and no lockfile raises with `repoRoot` and every candidate named.
  - *Evidence collected:* `pnpm --filter blogwright exec vitest run process-package-manager --reporter=verbose` → 5 passed: `detects pnpm|npm|yarn|bun from its lockfile` plus `raises naming the repo root and every lockfile it looked for when none matches`. Each positive case seeds exactly one lockfile into `createMemoryFileSystem`; the negative seeds only `README.md`.
  - *Checks:* the positive cases are generated by `it.each(Object.entries(PACKAGE_MANAGER_LOCKFILES))`, and the negative case asserts over `Object.values(PACKAGE_MANAGER_LOCKFILES)`; the thrown message is built from the same table (`process-package-manager.ts:82-84`). Adding a manager therefore adds a positive case and widens the expected message automatically - not hand-copied.
  - *Status:* SATISFIED

- **O3 - Process ownership and error translation.**
  - *Claim:* only the new adapter imports `node:child_process`, and its failures carry the command, the arguments and the directory.
  - *Evidence collected:* `grep -rn "node:child_process" packages/cli/src` → `adapters/process-vcs.ts:3,7`, `adapters/process-vcs.test.ts:7`, `adapters/process-package-manager.ts:5,10`; every path is under `packages/cli/src/adapters/`, already covered by the `.oxlintrc.json:71-84` override, so no new exception was added. `runPackageCommand` (`process-package-manager.ts:88-96`) is a line-for-line mirror of `runVcsCommand` (`process-vcs.ts:17-26`): `` `${command} ${args.join(' ')} failed in ${cwd}: ${(err as Error).message}` `` with `{ cause: err }`.
  - *Note:* `runVcsCommand` raises `maxBuffer` to a named `MAX_OUTPUT_BYTES`; `runPackageCommand` does not, so install output is capped at `execFile`'s 1 MiB default (see Defect D4).
  - *Status:* SATISFIED

- **O4 - Recording fake and the ports table.**
  - *Claim:* no test spawns a process, `createTestContext` defaults `ports.packages` to a recording fake, and DEVELOPMENT.md's ports table lists the port.
  - *Evidence collected:* `createRecordingPackageManager` at `packages/cli/src/test-support.ts:127-141`, defaulted at `:199` (`overrides.ports?.packages ?? createRecordingPackageManager()`). `grep -rn createProcessPackageManager` over the workspace hits only `context.ts:21,117`, the adapter, and `adapters/process-package-manager.test.ts`; that test exercises `detect` only, over `createMemoryFileSystem`, and contains no `execFile`/`spawn`. `pnpm test` → core 104, build-agent 27, pds 85, cli 136, all passing, no process spawned by the new tests. DEVELOPMENT.md:82 adds the `PackageManager` row naming `cli/src/ports.ts`, `createProcessPackageManager` and `createRecordingPackageManager`.
  - *Note:* "command tests assert on what the fake recorded" is vacuously true - this task adds no consumer. The corollary is that `add`/`remove` command construction has no test at all (Defect D2).
  - *Status:* SATISFIED

- **O5 - Meets the repo definition of done.**
  - *Claim:* tests written with the change pass, the lint/format/dead-code gates are clean, and limits are named constants or validated config fields.
  - *Evidence collected:* from the workspace root - `pnpm build` exit 0, `pnpm test` exit 0, `pnpm lint` exit 0 (pre-existing `no-shadow` warnings in `nodes.test.ts` only), `pnpm exec oxfmt --check .` exit 0 (125 files), `pnpm knip` exit 0. **`pnpm typecheck` exit 2** - `src/adapters/process-package-manager.test.ts(38,20)` and `(40,22)`: `TS2339: Property 'message' does not exist on type 'PackageManagerName | Error'`. DEVELOPMENT.md:317-318 names the five gates and records that CI adds `pnpm typecheck`; `.github/workflows/ci.yml` runs it immediately after `pnpm build`, so this change cannot go green in CI. No changeset is required (internal-only: no user-visible surface consumes the port yet). No magic numbers introduced.
  - *Status:* UNSATISFIED (typecheck gate red - Defect D1; and `add`/`remove` behaviour is untested - Defect D2)

- **O6 - Reviewer runs the detect tests and greps the process boundary (Reviewable).**
  - *Claim:* a reviewer can run `pnpm --filter blogwright test -- process-package-manager` and `grep -rn "node:child_process" packages/cli/src` and observe in-memory detection with no spawn, and a grep confined to `adapters/`.
  - *Evidence collected:* both commands run as written in the task file; test output and grep output recorded under O2 and O3. All five detect tests pass in 2ms against `createMemoryFileSystem`; all four grep paths are under `packages/cli/src/adapters/`.
  - *Status:* SATISFIED

## Regression check

- `packages/cli/src/deploy.test.ts` calls `createTestContext()` with no `ports` override → the full cli suite (16 files, 136 tests) passes with `ports.packages` present and never called : PRESERVED
- `packages/cli/src/adapters/process-vcs.test.ts` exercises `createProcessVcs` in a tmp dir → unchanged and passing; the second `execFile`-owning adapter shares no state : PRESERVED (but `process-vcs.ts:2-3` now carries a false comment - Defect D3)
- `packages/cli/src/context.ts:110` `createContext` builds the `Ports` object for every real CLI run → the only edit hoists `createNodeFileSystem()` into `const fs` and reuses that one instance for `ports.fs` and `createProcessPackageManager(fs)`; the factory performs no I/O at construction, so context creation is behaviourally identical : PRESERVED
- Full-`Ports` object literals exist only at `context.ts:113` and `test-support.ts:194`; both gained the field. `repo.ts` uses `Pick<Ports,'vcs'|'fs'>` and `core`'s `PluginPorts` is a structural subset, so widening `Ports` breaks neither : PRESERVED

## Residue

Notes for the validator: which managers count as "supported" is an implementation choice; the obligations require only that every entry in the module's lockfile table has a positive test and that the negative message is derived from that table. Whether `add` pins the CLI's own version is task 18's concern, not this task's. Bun's two historical lockfile names (`bun.lockb`, `bun.lock`) may both appear in the table; either way O2 requires a test per table entry.

Validator's addenda: only `bun.lock` is tabled, so a bun < 1.2 repo (`bun.lockb`) falls through to the "no supported package manager" error - contract-compliant, since the task named `bun.lock`. `add`/`remove` take no directory and resolve their own root via `findRepoRoot(fs)`, i.e. from ambient `process.cwd()`, while `detect` is parameterised on a `repoRoot` the caller chooses; that asymmetry is what the change spec prescribes, but it means a caller can detect against one root and install into another.

## Conclusion

VERDICT: pending correctness fix (O5 UNSATISFIED; O1-O4, O6 SATISFIED, all regressions PRESERVED)
CONFIDENCE: high
SUMMARY: The port, adapter, wiring, fake and docs are all correct and every regression is preserved, but the new adapter test does not typecheck (`TS2339` at `process-package-manager.test.ts:38,40`), so the CI `pnpm typecheck` gate is red and completeness is held pending that fix.
